apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrofin.fullname" . }}
  labels:
    {{- include "ferrofin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ferrofin.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
