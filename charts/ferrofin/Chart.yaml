apiVersion: v2
name: ferrofin
description: Official Helm chart for the Ferrofin media server — a from-scratch Rust implementation of the Jellyfin server API.
type: application
# Chart version. Bump on any chart change. CI publishes this to the OCI registry.
version: 0.2.0
# Tracks the ferrofin release the chart is validated against (kept in step with Cargo.toml).
appVersion: "0.2.0"
home: https://github.com/mangoleaf/ferrofin
sources:
  - https://github.com/mangoleaf/ferrofin
keywords:
  - ferrofin
  - jellyfin
  - media
  - self-hosted
