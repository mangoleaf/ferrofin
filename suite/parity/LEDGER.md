# Ferrofin ⇄ Jellyfin parity ledger

_Generated by `parity/gen-ledger.py` — do not hand-edit; edit `parity/seed.json` or the classify overlay and regenerate._

**41/412 deep-verified · 188 verified another way · 102 classified-divergence · 4 open work · 41 flagged (unreviewed) · 36 untested**  
_Layer 1: 187/206 status-conformant · 129/131 schema-valid_

_deep-verified means exactly one thing: the response (or a write's read-back) was itself diffed against Jellyfin 10.11.8 — every non-volatile field, or the served file's sha256 — and came back clean. It is the ONLY method counted in that number. Every other verified row declares which weaker thing it established — see the table below — and no row may reach this ledger without declaring a method (`gen-ledger.py --check` rejects it)._

## How each row was verified

_One closed set, defined in `parity/verification.py`. A row's method is the claim it earns; nothing is inferred and nothing defaults._

| | method | ops | what was actually compared |
|---|---|---:|---|
| ✅ | `body-diff` — deep-verified **(the headline)** | 41 | the response (or a write's read-back) was itself diffed against Jellyfin 10.11.8 — every non-volatile field, or the served file's sha256 — and came back clean |
| ◐ | `property` — property-verified | 14 | named properties derived from the response agreed on both servers (decoded format/dimensions, media type, container signature, normalised playlist, named invariants); the bodies were NOT diffed |
| ⊙ | `effect` — effect-verified | 97 | a write was applied and its effect confirmed on each server's own read-back; no response body was diffed and the two servers' responses were never compared with each other |
| ▫ | `status-class` — status-class-only | 54 | only the HTTP status class agreed (at most also a content-type family) — nothing was served, or a deliberately-bogus id was refused by both |
| ∅ | `empty-corpus` — empty-corpus | 23 | both servers returned an EMPTY result set, so only the empty envelope's own zeros were compared — an unconditional-empty handler would pass identically |
| · | _untested_ | 36 | no probe produced a verdict — including probes that ran and compared nothing, which is an absence of evidence, not a pass |

## Depth (what the wired handler actually does)

| depth | ops | % |
|---|---:|---:|
| REAL | 412 | 100% |
| PARTIAL | 0 | 0% |
| HOLLOW | 0 | 0% |
| STUB | 0 | 0% |
| **route registered** | 412 | 100% |
| **route 501-stub** | 0 | 0% |

## Ownership (core vs compiled-in extensions)

_Extensions must not dilute or flatter core's coverage number — each owner's deep-verified share stands alone._

| owner | ops | deep-verified | verified another way | classified | open work | flagged | untested |
|---|---:|---:|---:|---:|---:|---:|---:|
| core | 389 | 41 (10%) | 188 | 79 | 4 | 41 | 36 |
| file-transformation | 1 | 0 (0%) | 0 | 1 | 0 | 0 | 0 |
| intro-skipper | 18 | 0 (0%) | 0 | 18 | 0 | 0 | 0 |
| merge-versions | 4 | 0 (0%) | 0 | 4 | 0 | 0 | 0 |

## Deep-verified (response + read-back diffed clean vs Jellyfin 10.11.8)

_The headline. Nothing else on this page is counted in it._  
_The parenthesis is HOW MUCH was compared — the number of non-volatile leaves the diff actually walked. A clean diff of one leaf and a clean diff of 984 are both real and they are not the same evidence, so the page says which it was._

- ✅ `GET /Audio/{itemId}/stream` _(sha256 of the served bytes)_
- ✅ `GET /Audio/{itemId}/stream.{container}` _(sha256 of the served bytes)_
- ✅ `GET /Audio/{itemId}/universal` _(sha256 of the served bytes)_
- ✅ `GET /Auth/PasswordResetProviders` _(1 field compared)_
- ✅ `GET /Auth/Providers` _(1 field compared)_
- ✅ `GET /Branding/Configuration` _(3 fields compared)_
- ✅ `GET /Devices/Info` _(7 fields compared)_
- ✅ `GET /Environment/DirectoryContents` _(1500 fields compared)_
- ✅ `GET /Environment/ParentPath` _(1 field compared)_
- ✅ `GET /Items/{itemId}/Download` _(sha256 of the served bytes)_
- ✅ `GET /Items/{itemId}/File` _(sha256 of the served bytes)_
- ✅ `GET /LiveTv/ListingProviders/Default` _(15 fields compared)_
- ✅ `GET /LiveTv/ListingProviders/SchedulesDirect/Countries` _(238 fields compared)_
- ✅ `GET /LiveTv/Timers/Defaults` _(24 fields compared)_
- ✅ `GET /Localization/Countries` _(556 fields compared)_
- ✅ `GET /Localization/Cultures` _(984 fields compared)_
- ✅ `GET /Localization/Options` _(142 fields compared)_
- ✅ `GET /Localization/ParentalRatings` _(218 fields compared)_
- ✅ `GET /Persons` _(29 fields compared)_
- ✅ `GET /QuickConnect/Enabled` _(1 field compared)_
- ✅ `GET /ScheduledTasks/{taskId}` _(9 fields compared)_
- ✅ `GET /Sessions` _(32 fields compared)_
- ✅ `GET /Shows/{itemId}/Similar` _(14 fields compared)_ — flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. SERIES SEED: the pre-A2 server ran the weighted MOVIE scorer for every kind, which credits a shared studio (+5) and shared people and pulled genre-unrelated series in (F ['Series 06','Series 03','Series 05','Series 07'] vs J ['Series 06']); `SeriesSimilarItemsProvider` filters on Genres/Tags alone, and porting it made the set match. The probe also exposed a DTO gap since fixed: Ferrofin omitted `AirDays` on Series where 10.11.8 always sends `[]` (v10.11.8 DtoService.cs:1243 assigns `series.AirDays`, and v10.11.8 Series.cs:31 initialises it to `Array.Empty<DayOfWeek>()` while no column persists it) — the null-where-Jellyfin-sends-non-null shape strict clients crash on.
- ✅ `GET /Shows/{seriesId}/Episodes` _(53 fields compared)_
- ✅ `GET /Shows/{seriesId}/Seasons` _(15 fields compared)_
- ✅ `GET /Startup/Configuration` _(3 fields compared)_
- ✅ `GET /Startup/FirstUser` _(1 field compared)_
- ✅ `GET /Startup/User` _(1 field compared)_
- ✅ `GET /Studios` _(20 fields compared)_
- ✅ `GET /System/Configuration` _(86 fields compared)_
- ✅ `GET /System/Endpoint` _(2 fields compared)_
- ✅ `GET /System/Info` _(9 fields compared)_
- ✅ `GET /System/Info/Public` _(1 field compared)_
- ✅ `GET /Tmdb/ClientConfiguration` _(28 fields compared)_
- ✅ `GET /Users` _(48 fields compared)_
- ✅ `GET /Users/Me` _(48 fields compared)_
- ✅ `GET /Videos/{itemId}/stream` _(sha256 of the served bytes)_
- ✅ `GET /Videos/{itemId}/stream.{container}` _(sha256 of the served bytes)_
- ✅ `GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/Stream.{routeFormat}` _(exact decoded text)_
- ✅ `GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/{routeStartPositionTicks}/Stream.{routeFormat}` _(exact decoded text)_
- ✅ `GET /Videos/{videoId}/{mediaSourceId}/Attachments/{index}` _(sha256 of the served bytes)_

## Property-verified — ◐ `property` (NOT deep-verified)

_Named properties derived from the response agreed on both servers (decoded format/dimensions, media type, container signature, normalised playlist, named invariants); the bodies were NOT diffed. A real verification and a weaker one: never counted in the deep-verified number._

- ◐ `GET /Audio/{itemId}/hls1/{playlistId}/{segmentId}.{container}`
- ◐ `GET /Audio/{itemId}/main.m3u8`
- ◐ `GET /Audio/{itemId}/master.m3u8`
- ◐ `GET /Genres/{name}/Images/{imageType}`
- ◐ `GET /Genres/{name}/Images/{imageType}/{imageIndex}`
- ◐ `GET /LiveTv/LiveRecordings/{recordingId}/stream`
- ◐ `GET /LiveTv/LiveStreamFiles/{streamId}/stream.{container}`
- ◐ `GET /Playback/BitrateTest`
- ◐ `GET /System/Logs/Log`
- ◐ `GET /Videos/{itemId}/hls1/{playlistId}/{segmentId}.{container}`
- ◐ `GET /Videos/{itemId}/live.m3u8`
- ◐ `GET /Videos/{itemId}/main.m3u8`
- ◐ `GET /Videos/{itemId}/master.m3u8`
- ◐ `GET /Videos/{itemId}/{mediaSourceId}/Subtitles/{index}/subtitles.m3u8`

## Effect-verified — ⊙ `effect` (NOT deep-verified)

_A write was applied and its effect confirmed on each server's own read-back; no response body was diffed and the two servers' responses were never compared with each other. A real verification and a weaker one: never counted in the deep-verified number._

- ⊙ `POST /Auth/Keys`
- ⊙ `DELETE /Auth/Keys/{key}`
- ⊙ `GET /Backup`
- ⊙ `POST /Backup/Create`
- ⊙ `GET /Backup/Manifest`
- ⊙ `POST /Backup/Restore`
- ⊙ `DELETE /Branding/Splashscreen`
- ⊙ `GET /Branding/Splashscreen`
- ⊙ `POST /Branding/Splashscreen`
- ⊙ `POST /ClientLog/Document`
- ⊙ `POST /Collections`
- ⊙ `DELETE /Collections/{collectionId}/Items`
- ⊙ `POST /Collections/{collectionId}/Items`
- ⊙ `DELETE /Devices`
- ⊙ `GET /Devices/Options`
- ⊙ `POST /Devices/Options`
- ⊙ `POST /DisplayPreferences/{displayPreferencesId}`
- ⊙ `DELETE /Items/{itemId}`
- ⊙ `POST /Items/{itemId}`
- ⊙ `POST /Items/{itemId}/Images/{imageType}`
- ⊙ `DELETE /Items/{itemId}/Images/{imageType}/{imageIndex}`
- ⊙ `POST /Items/{itemId}/Images/{imageType}/{imageIndex}`
- ⊙ `POST /Items/{itemId}/Images/{imageType}/{imageIndex}/Index`
- ⊙ `POST /Items/{itemId}/PlaybackInfo`
- ⊙ `DELETE /Library/VirtualFolders`
- ⊙ `POST /Library/VirtualFolders`
- ⊙ `POST /Library/VirtualFolders/Name`
- ⊙ `DELETE /Library/VirtualFolders/Paths`
- ⊙ `POST /Library/VirtualFolders/Paths`
- ⊙ `POST /Library/VirtualFolders/Paths/Update`
- ⊙ `POST /LiveStreams/Close`
- ⊙ `POST /LiveStreams/Open`
- ⊙ `POST /LiveTv/ListingProviders`
- ⊙ `GET /LiveTv/Programs/{programId}`
- ⊙ `DELETE /LiveTv/Recordings/{recordingId}`
- ⊙ `GET /LiveTv/Recordings/{recordingId}`
- ⊙ `POST /LiveTv/Timers`
- ⊙ `DELETE /LiveTv/Timers/{timerId}`
- ⊙ `GET /LiveTv/Timers/{timerId}`
- ⊙ `POST /LiveTv/TunerHosts`
- ⊙ `DELETE /PlayingItems/{itemId}`
- ⊙ `POST /PlayingItems/{itemId}`
- ⊙ `POST /PlayingItems/{itemId}/Progress`
- ⊙ `POST /Playlists`
- ⊙ `GET /Playlists/{playlistId}`
- ⊙ `DELETE /Playlists/{playlistId}/Items`
- ⊙ `GET /Playlists/{playlistId}/Items`
- ⊙ `POST /Playlists/{playlistId}/Items`
- ⊙ `POST /Playlists/{playlistId}/Items/{itemId}/Move/{newIndex}`
- ⊙ `GET /Playlists/{playlistId}/Users`
- ⊙ `GET /Playlists/{playlistId}/Users/{userId}`
- ⊙ `POST /Playlists/{playlistId}/Users/{userId}`
- ⊙ `GET /QuickConnect/Connect`
- ⊙ `POST /QuickConnect/Initiate`
- ⊙ `POST /ScheduledTasks/{taskId}/Triggers`
- ⊙ `POST /Sessions/Capabilities`
- ⊙ `POST /Sessions/Capabilities/Full`
- ⊙ `POST /Sessions/Logout`
- ⊙ `POST /Sessions/Playing`
- ⊙ `POST /Sessions/Playing/Progress`
- ⊙ `POST /Sessions/Playing/Stopped`
- ⊙ `DELETE /Sessions/{sessionId}/User/{userId}`
- ⊙ `POST /Sessions/{sessionId}/User/{userId}`
- ⊙ `POST /Startup/Complete`
- ⊙ `POST /Startup/Configuration`
- ⊙ `POST /Startup/RemoteAccess`
- ⊙ `POST /System/Configuration`
- ⊙ `POST /System/Configuration/Branding`
- ⊙ `GET /System/Configuration/{key}`
- ⊙ `POST /System/Configuration/{key}`
- ⊙ `POST /System/Restart`
- ⊙ `POST /System/Shutdown`
- ⊙ `DELETE /UserFavoriteItems/{itemId}`
- ⊙ `POST /UserFavoriteItems/{itemId}`
- ⊙ `DELETE /UserImage`
- ⊙ `POST /UserImage`
- ⊙ `DELETE /UserItems/{itemId}/Rating`
- ⊙ `POST /UserItems/{itemId}/Rating`
- ⊙ `POST /UserItems/{itemId}/UserData`
- ⊙ `DELETE /UserPlayedItems/{itemId}`
- ⊙ `POST /UserPlayedItems/{itemId}`
- ⊙ `POST /Users`
- ⊙ `POST /Users/AuthenticateByName`
- ⊙ `POST /Users/AuthenticateWithQuickConnect`
- ⊙ `POST /Users/Configuration`
- ⊙ `POST /Users/ForgotPassword`
- ⊙ `POST /Users/ForgotPassword/Pin`
- ⊙ `POST /Users/New`
- ⊙ `POST /Users/Password`
- ⊙ `DELETE /Users/{userId}`
- ⊙ `GET /Users/{userId}`
- ⊙ `POST /Users/{userId}/Policy`
- ⊙ `DELETE /Videos/ActiveEncodings`
- ⊙ `POST /Videos/MergeVersions`
- ⊙ `DELETE /Videos/{itemId}/AlternateSources`
- ⊙ `POST /Videos/{itemId}/Subtitles`
- ⊙ `DELETE /Videos/{itemId}/Subtitles/{index}`

## Status-class-only — ▫ `status-class` (NOT deep-verified)

_Only the HTTP status class agreed (at most also a content-type family) — nothing was served, or a deliberately-bogus id was refused by both. A real verification and a weaker one: never counted in the deep-verified number._

- ▫ `GET /Artists/{name}/Images/{imageType}/{imageIndex}`
- ▫ `HEAD /Artists/{name}/Images/{imageType}/{imageIndex}`
- ▫ `GET /Audio/{itemId}/hls/{segmentId}/stream.aac`
- ▫ `GET /Audio/{itemId}/hls/{segmentId}/stream.mp3`
- ▫ `HEAD /Audio/{itemId}/master.m3u8`
- ▫ `HEAD /Audio/{itemId}/stream`
- ▫ `HEAD /Audio/{itemId}/stream.{container}`
- ▫ `HEAD /Audio/{itemId}/universal`
- ▫ `GET /Branding/Css`
- ▫ `GET /Branding/Css.css`
- ▫ `POST /Environment/ValidatePath`
- ▫ `GET /FallbackFont/Fonts/{name}`
- ▫ `HEAD /Genres/{name}/Images/{imageType}`
- ▫ `HEAD /Genres/{name}/Images/{imageType}/{imageIndex}`
- ▫ `DELETE /Items`
- ▫ `POST /Items/{itemId}/ContentType`
- ▫ `HEAD /Items/{itemId}/Images/{imageType}`
- ▫ `HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}`
- ▫ `HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}`
- ▫ `POST /Items/{itemId}/Refresh`
- ▫ `POST /Library/Refresh`
- ▫ `GET /MusicGenres/{name}/Images/{imageType}`
- ▫ `HEAD /MusicGenres/{name}/Images/{imageType}`
- ▫ `GET /MusicGenres/{name}/Images/{imageType}/{imageIndex}`
- ▫ `HEAD /MusicGenres/{name}/Images/{imageType}/{imageIndex}`
- ▫ `GET /Persons/{name}/Images/{imageType}`
- ▫ `HEAD /Persons/{name}/Images/{imageType}`
- ▫ `GET /Persons/{name}/Images/{imageType}/{imageIndex}`
- ▫ `HEAD /Persons/{name}/Images/{imageType}/{imageIndex}`
- ▫ `POST /Playlists/{playlistId}`
- ▫ `POST /QuickConnect/Authorize`
- ▫ `DELETE /ScheduledTasks/Running/{taskId}`
- ▫ `POST /ScheduledTasks/Running/{taskId}`
- ▫ `POST /Sessions/Playing/Ping`
- ▫ `POST /Sessions/Viewing`
- ▫ `POST /Sessions/{sessionId}/Command`
- ▫ `POST /Sessions/{sessionId}/Command/{command}`
- ▫ `POST /Sessions/{sessionId}/Message`
- ▫ `POST /Sessions/{sessionId}/Playing`
- ▫ `POST /Sessions/{sessionId}/Playing/{command}`
- ▫ `POST /Sessions/{sessionId}/System/{command}`
- ▫ `POST /Sessions/{sessionId}/Viewing`
- ▫ `GET /Studios/{name}/Images/{imageType}`
- ▫ `HEAD /Studios/{name}/Images/{imageType}`
- ▫ `GET /Studios/{name}/Images/{imageType}/{imageIndex}`
- ▫ `HEAD /Studios/{name}/Images/{imageType}/{imageIndex}`
- ▫ `POST /System/Ping`
- ▫ `GET /UserImage`
- ▫ `HEAD /UserImage`
- ▫ `GET /Videos/{itemId}/hls/{playlistId}/stream.m3u8`
- ▫ `GET /Videos/{itemId}/hls/{playlistId}/{segmentId}.{segmentContainer}`
- ▫ `HEAD /Videos/{itemId}/master.m3u8`
- ▫ `HEAD /Videos/{itemId}/stream`
- ▫ `HEAD /Videos/{itemId}/stream.{container}`

## Empty-corpus — ∅ `empty-corpus` (NOT deep-verified)

_Both servers returned an EMPTY result set, so only the empty envelope's own zeros were compared — an unconditional-empty handler would pass identically. A real verification and a weaker one: never counted in the deep-verified number._

- ∅ `GET /Albums/{itemId}/InstantMix`
- ∅ `GET /Albums/{itemId}/Similar` — flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. GREEN BUT VACUOUS ON THE CURRENT FIXTURE — read this row as 'no divergence detected', not as 'verified'. `suite/perf/gen-fixtures.sh` gave each of the 3 albums a UNIQUE music genre, so the candidate universe is empty by construction on BOTH servers and the leg diffs `{"Items": [], "TotalRecordCount": 0}` against itself. The fixture is changed (two of the three artists now share a genre) but that only takes effect at the campaign's final wipe-and-provision; until that sweep runs, the album matching itself is covered by the `an_album_seed_matches_albums_sharing_a_genre_only` unit test (two albums sharing a genre, a same-genre track that must NOT come back because IncludeItemTypes is [MusicAlbum] alone, and the ExcludeArtistIds leg with a positive control). A real Ferrofin bug found behind this row IS fixed and verified live: album Genres/Studios/PremiereDate are inherited from the album's Audio children (C# AlbumMetadataService + MetadataService.UpdateGenres/UpdateStudios/UpdatePremiereDate); Ferrofin aggregated only album-artist and year, so every album had `Genres: []` and was unreachable by the Genres= query this endpoint issues. After the fix, F album Genres/PremiereDate/ProductionYear match J exactly (Jazz/Rock/Ambient, 2022-01-01T00:00:00.0000000Z, 2022).
- ∅ `GET /Artists/{itemId}/InstantMix`
- ∅ `GET /Auth/Keys`
- ∅ `GET /Channels`
- ∅ `GET /Channels/Items/Latest`
- ∅ `GET /Items/{itemId}/CriticReviews`
- ∅ `GET /Items/{itemId}/InstantMix`
- ∅ `GET /Items/{itemId}/Intros`
- ∅ `GET /Items/{itemId}/ThemeMedia`
- ∅ `GET /Items/{itemId}/ThemeSongs`
- ∅ `GET /Items/{itemId}/ThemeVideos`
- ∅ `GET /LiveTv/Recordings/Groups`
- ∅ `GET /LiveTv/Recordings/Series`
- ∅ `GET /LiveTv/SeriesTimers`
- ∅ `GET /LiveTv/Timers`
- ∅ `GET /MediaSegments/{itemId}`
- ∅ `GET /MusicGenres/{name}/InstantMix`
- ∅ `GET /Shows/NextUp`
- ∅ `GET /Shows/Upcoming`
- ∅ `GET /Songs/{itemId}/InstantMix`
- ∅ `GET /UserItems/Resume`
- ∅ `GET /Videos/{itemId}/AdditionalParts`

## Classified divergence (accepted — not a bug)

_Settled decisions. An item here is a question that has been closed._

- ⚠️ `GET /Channels/{channelId}/Features` — requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers.
- ⚠️ `GET /Channels/{channelId}/Items` — requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers.
- ⚠️ `GET /Environment/Drives` — instance: per-host filesystem drives.
- ⚠️ `GET /Episode/{Id}/Timestamps` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /Episode/{Id}/Timestamps` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Episode/{id}/IntroSkipperSegments` — expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming.
- ⚠️ `POST /FileTransformation/RegisterTransformation` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Genres` — verified 2026-08-11: QueryResult<BaseItemDto> (Items array + TotalRecordCount + StartIndex), status+schema conformant; empty-library returns the well-formed empty result.
- ⚠️ `GET /Genres/{genreName}` — accepted-divergence: same no-lazy-create-on-GET family as /Studios/{name}. Existing genre→200 BaseItemDto (both servers), absent→Ferrofin 404 vs Jellyfin auto-created 200. Shape verified.
- ⚠️ `GET /IntroSkipper` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /IntroSkipper/SupportBundle` — expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (H=200 vs J=404). By design.
- ⚠️ `POST /Intros/AnalyzerActions/UpdateSeason` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Intros/AnalyzerActions/{SeasonId}` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /Intros/EraseTimestamps` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /Intros/RebuildDatabase` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /Intros/ScanSeason/{SeriesId}/{SeasonId}` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Intros/ScanStatus` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `DELETE /Intros/Show/{SeriesId}/{SeasonId}` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Intros/Show/{SeriesId}/{SeasonId}` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Items/{itemId}` — jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. NOTE: the previous 'instance/probe-noise' text on this row was DISPROVEN in batch A3 — running both containers' ffprobe binaries over the same file gives byte-identical JSON (7.1.4 vs 7.1.3-Jellyfin), and ffprobe emits no per-stream bit_rate at all. The five other residuals it covered were real server bugs and are fixed: MediaSources[].Name now ports BaseItem.GetMediaSourceName (file stem, not item.Name); codec_tag_string binds the correct 10.11.8 JSON key (the trailing-'?' typo is master-only); Level/IsAVC are set on every stream and subtitle Width/Height default to 0, matching C#'s non-nullable probing DTO; LocalizedLanguage is no longer serialized (off-contract, the cause of schema_valid:false); and the People[].Role read projection no longer NULLIFs the empty string C# always emits.
- ⚠️ `GET /Items/{itemId}/PlaybackInfo` — jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. Same root cause and same batch-A3 fixes as GET /Items/{itemId}; the 'instance/probe-noise' text previously on this row was equally disproven.
- ⚠️ `POST /Items/{itemId}/RemoteSearch/Subtitles/{subtitleId}` — remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live.
- ⚠️ `GET /Items/{itemId}/Similar` — instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93).
- ⚠️ `POST /Jellyfin.Plugin.OpenSubtitles/ValidateLoginInfo` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /Libraries/AvailableOptions` — expected-extension (SupportedImageTypes + the fetcher lists): Ferrofin compiles in TheTVDB, FanArt, Open Subtitles and IntroSkipper, which the lab's stock Jellyfin 10.11.8 does not ship (GET /Plugins there lists only AudioDB and MusicBrainz). They therefore appear as extra ImageFetchers/MetadataFetchers rows, and — because SupportedImageTypes is the flattened union of every compiled image provider's GetSupportedImages (ProviderManager.cs:706-714) — their image types (Art/Disc/Banner) widen that list beyond Jellyfin's. The 2026-08-11 'verified' text this replaces was false: sweep already recorded 15 extra / 39 mismatch / 2 missing paths and schema_valid:false on this row. Fixed in batch A3: DefaultImageOptions is now the verbatim TypeOptions.DefaultImageOptions table (order included, [] for unnamed types); SupportedImageTypes is derived from the providers instead of a hardcoded 11-element enum dump; isNewLibrary now drives DefaultEnabled; and SimilarItemProviders (post-10.11.8, additionalProperties:false) is no longer serialized. OPEN WORK, not accepted: upstream's 'Screen Grabber' (VideoImageProvider, Order 100, [Primary]) is still unported — Ferrofin owns the capability (MediaEncoder::extract_video_image) but does not register the provider, so it is deliberately absent from the list rather than advertised hollow.
- ⚠️ `GET /LiveTv/ChannelMappingOptions` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider.
- ⚠️ `POST /LiveTv/ChannelMappings` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider.
- ⚠️ `GET /LiveTv/GuideInfo` — instance: now-relative guide window (now .. now+7d) matches Jellyfin's behavior, but the exact StartDate/EndDate timestamps can't match across two independently-clocked servers.
- ⚠️ `DELETE /LiveTv/ListingProviders` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture's listings provider and observes the guide emptying.
- ⚠️ `GET /LiveTv/ListingProviders/Lineups` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a lineup probe (needs a Schedules Direct-style provider; xmltv has none).
- ⚠️ `POST /LiveTv/Programs` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a programmes-by-body probe (the POST form of /LiveTv/Programs).
- ⚠️ `GET /LiveTv/Recordings/Groups/{groupId}` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a recordings-group probe once a recording exists.
- ⚠️ `POST /LiveTv/SeriesTimers` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete).
- ⚠️ `DELETE /LiveTv/SeriesTimers/{timerId}` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete).
- ⚠️ `GET /LiveTv/SeriesTimers/{timerId}` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete).
- ⚠️ `POST /LiveTv/SeriesTimers/{timerId}` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete).
- ⚠️ `POST /LiveTv/Timers/{timerId}` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs an update step in the timer journey (change pre-padding, read back).
- ⚠️ `DELETE /LiveTv/TunerHosts` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture tuner and observes its channels going.
- ⚠️ `POST /LiveTv/Tuners/{tunerId}/Reset` — requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a tuner-reset probe with the fixture tuner's id.
- ⚠️ `GET /MediaSegmentsApi` — expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming.
- ⚠️ `POST /MediaSegmentsApi/{itemId}` — expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming.
- ⚠️ `DELETE /MediaSegmentsApi/{segmentId}` — expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming.
- ⚠️ `POST /MergeVersions/MergeEpisodes` — expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design.
- ⚠️ `POST /MergeVersions/MergeMovies` — expected-extension: Ferrofin ships the MergeVersions controller (danieladov 'Merge Versions' plugin surface) compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design.
- ⚠️ `POST /MergeVersions/SplitEpisodes` — expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design.
- ⚠️ `POST /MergeVersions/SplitMovies` — expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design.
- ⚠️ `GET /Movies/{itemId}/Similar` — instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93).
- ⚠️ `GET /Packages` — instance: aggregates the enabled repositories' manifests (PackageInfo[]) plus synthesized compiled-in entries; repositoryName/Url stamped from the repository actually fetched (manifest claims are untrusted); manifest fetch is size-capped + timed out. Catalog content is repository-config-specific — harness instances configure none — so nothing to diff. Verified 2026-08-14 by unit + server HTTP tests.
- ⚠️ `POST /Packages/Installed/{name}` — accepted-divergence: repository install of WASM plugins is real (2026-08-14: ABI gate → HTTPS-per-hop download → streamed size cap → sha256/md5 verify → component+identity validation → guid-squat refusal → atomic stage → restart-required; admin-only; end-to-end server test). Divergence vs Jellyfin: the install is SYNCHRONOUS — none of the PackageInstalling/Completed/Failed/Cancelled WebSocket messages are emitted (no dashboard progress; a slow repo holds the POST open), and the plugin appears in /Plugins only after the activating restart. Async install + Package* push is the recorded follow-up (WebSocket-push backlog).
- ⚠️ `DELETE /Packages/Installing/{packageId}` — accepted-divergence: always 404 — installs are synchronous, so no tracked installation exists to cancel (Jellyfin tracks async installs). Admin-only.
- ⚠️ `GET /Packages/{name}` — instance: resolves a package by name/assemblyGuid from the aggregated repository catalog — catalog content is repository-config-specific, and the harness instances configure none, so there is no package to diff. Route verified 2026-08-14 by unit + server HTTP tests.
- ⚠️ `GET /Persons/{name}` — accepted-divergence: same no-lazy-create-on-GET family as /Studios/{name}. Existing person→200 BaseItemDto, absent→404 (both servers 404 in the harness run). Shape verified.
- ⚠️ `GET /Playlists/{itemId}/InstantMix` — expected-extension: Ferrofin generates an instant mix for a playlist (H=200); stock Jellyfin returns 404 (playlists are not an instant-mix source). A harmless superset.
- ⚠️ `DELETE /Playlists/{playlistId}/Users/{userId}` — instance/methodology: Ferrofin correctly removes the playlist share (H read-back shows it gone); Jellyfin returns 204 but still lists the user (Jellyfin quirk), so Ferrofin is the more-correct side — not a defect.
- ⚠️ `GET /Plugins` — expected-extension: lists Ferrofin's installed plugins (compiled-in extensions + staged WASM plugins) — the set necessarily differs from any stock Jellyfin instance. Route is real and schema-conformant (server HTTP tests).
- ⚠️ `DELETE /Plugins/{pluginId}` — accepted-divergence: real uninstall for a staged WASM plugin (file + config + enabled override removed, restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only.
- ⚠️ `GET /Plugins/{pluginId}/Configuration` — instance: the two servers share no plugin id (Ferrofin ships compiled-in extensions + WASM plugins; stock Jellyfin ships neither), so there is no config to fetch on both sides. Real read (stored values overlaid on defaults, C# partial-deserialize semantics); verified 2026-08-14 by unit + handler tests.
- ⚠️ `POST /Plugins/{pluginId}/Configuration` — instance: no shared plugin id across the two servers, so no config write to diff. Real write (JSON-validated, persisted; admin-only — a WASM plugin's config is guest input); verified 2026-08-14 by unit + handler tests incl. 403.
- ⚠️ `POST /Plugins/{pluginId}/Manifest` — instance: no shared plugin id across the two servers, so no manifest to fetch on both sides. Real read (GetPluginManifest port); verified 2026-08-14 by handler tests.
- ⚠️ `DELETE /Plugins/{pluginId}/{version}` — accepted-divergence: real uninstall for a staged WASM plugin (restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only.
- ⚠️ `POST /Plugins/{pluginId}/{version}/Disable` — accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window.
- ⚠️ `POST /Plugins/{pluginId}/{version}/Enable` — accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window.
- ⚠️ `GET /Plugins/{pluginId}/{version}/Image` — instance: no shared plugin id across the two servers, so no image to fetch on both sides. Real read (bundled image bytes + content type); verified 2026-08-14 by handler tests.
- ⚠️ `GET /Providers/Subtitles/Subtitles/{subtitleId}` — remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live.
- ⚠️ `GET /Repositories` — instance: returns the admin-configured package repositories — instance-local state (harness instances configure none), nothing to diff. Verified 2026-08-14 by unit + handler tests.
- ⚠️ `POST /Repositories` — instance: replaces the configured package repositories (admin-only, RequiresElevation port) — instance-local state, nothing to diff. Verified 2026-08-14 by unit + handler tests incl. 403 for non-admin.
- ⚠️ `GET /ScheduledTasks` — instance: LastExecutionResult depends on whether/when a task last ran.
- ⚠️ `POST /SkipButtonCss/InjectCss` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /SkipButtonCss/UpdateSkipDuration` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `POST /Startup/User` — accepted-divergence: post-setup the first user already has a password and the contract is 403 (the Forbid guard upstream added in v12, ported by Ferrofin); Jellyfin 10.11.8 predates it and answers 204, silently re-setting the admin password. The journey asserts the correct contract (H=403 J=204).
- ⚠️ `GET /Studios/{name}` — accepted-divergence: Ferrofin does not lazily CREATE a by-name item on a GET (Jellyfin's GetItemByName writes a row as a read side-effect → J=200 for an absent studio). Ferrofin returns the item if it exists (200, BaseItemDto) else 404 — a GET with no filesystem write. Verified live 2026-08-11: existing→200, absent→404.
- ⚠️ `POST /SyncPlay/Buffering` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Join` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Leave` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/MovePlaylistItem` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/New` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/NextItem` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Pause` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Ping` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/PreviousItem` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Queue` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Ready` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/RemoveFromPlaylist` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Seek` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/SetIgnoreWait` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/SetNewQueue` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/SetPlaylistItem` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/SetRepeatMode` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/SetShuffleMode` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Stop` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `POST /SyncPlay/Unpause` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `GET /SyncPlay/{id}` — verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py).
- ⚠️ `GET /System/ActivityLog/Entries` — instance: activity-log entries differ per instance/run.
- ⚠️ `GET /System/Info/Storage` — instance: per-host disk paths and free/used space.
- ⚠️ `GET /System/Logs` — instance: log-file set differs per instance/run.
- ⚠️ `GET /System/Ping` — instance: returns the server name string (ferrofin vs the Jellyfin instance name).
- ⚠️ `GET /Trailers` — instance: Jellyfin 10.11.8 returns 500 here (its own error with no Trailers provider), while Ferrofin correctly returns 200 with an empty result. Kept correct per policy (do not port Jellyfin bugs); not a Ferrofin gap.
- ⚠️ `GET /Trailers/{itemId}/Similar` — instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93).
- ⚠️ `GET /Years` — expected-extension: Ferrofin does not materialize Year items (documented out-of-scope).
- ⚠️ `GET /Years/{year}` — expected-extension: Ferrofin does not materialize Year items (documented out-of-scope; matches the GET /Years classification), so a single year has no item to return (H=404 vs J=200).
- ⚠️ `GET /web/ConfigurationPage` — expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises.
- ⚠️ `GET /web/ConfigurationPages` — expected-extension: plugin configuration pages come from external plugins Ferrofin doesn't host.

## Open work (NOT accepted — a named divergence still to port)

_These are real gaps with an owner and a path, kept OUT of the accepted section: rendering an unfinished port under "accepted" is how a gap quietly becomes a decision._

- ⛏ `GET /Artists/{itemId}/Similar` — open-work (NOT accepted): Ferrofin returns 0 rows where Jellyfin returns 2. The scorer half is fixed — the seed's kind now selects the C# provider shape (`MusicArtistSimilarItemsProvider` is a flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [MusicArtist] }`, and an EMPTY Genres/Tags pair is not a predicate, so an untagged artist must match every other artist; covered by the `an_untagged_artist_seed_still_matches_every_other_artist` unit test). What still blocks it is the item-UNIVERSE divergence, not the similarity code: Ferrofin materializes MusicArtist as a parentless by-name row with a NULL TopParentId, and every user-scoped `InternalItemsQuery` is scoped to the user's views via `TopParentId IS NOT NULL` (item_repository.rs `scope_to_user_libraries`, the port of C# `AddUserToQuery`), so no artist is reachable by any query that does not name it by id. Detector, both servers: `/Items?userId=<u>&recursive=true&includeItemTypes=MusicArtist` -> F 0, J 3, while `/Artists?userId=<u>` returns all 3 on both, and adding `&ids=<artistId>` makes the same query return the row on Ferrofin. Same root cause as the MusicArtist/Folder/TvChannel/UserView rows missing from GET /Items/Suggestions. Un-defer path: give the MusicArtist by-name row a TopParentId (Jellyfin's artist rows are physical folder items under the music library, which is why they survive `AddUserToQuery`) — this changes the recursive item universe server-wide, so it needs its own change with the perf gate, not a ride-along. ORDERING, for the record: this endpoint's provider orders `Random` on both servers (MusicArtistSimilarItemsProvider.cs:50 / v10.11.8 LibraryController.cs:801) and Ferrofin now emits `Random` too, so when the universe bug is fixed the reads leg must stay pinned at a limit above the candidate pool — the ORDER is not gateable by any probe, since parity_diff aligns arrays by key.
- ⛏ `GET /Items/Suggestions` — open-work (NOT accepted). The old text ('Random, so the set is non-deterministic') was only half true and it hid two real divergences. Random here PERMUTES but does not change the set: `SuggestionsController` runs ONE query (`OrderBy = [(Random, Descending)]`, Recursive, mediaType/type, IsVirtualItem=false) and Ferrofin's handler is a line-for-line port, so at `limit >= |universe|` the answer is fully determined and parity_diff aligns Items by Path. The probe was the problem: `limit=10` sampled 10 of 552 at random and could never diff. It is now eight legs: seven pinned at a full limit (per type), where Random only permutes and the whole body IS diffed, plus one narrow startIndex/limit leg. That narrow leg is the one place a projection is applied — a page of 3 out of 500 Random-ordered rows draws a different triple per call on BOTH servers (measured: three consecutive calls, three different triples each side), so it compares StartIndex, TotalRecordCount and the page length only. Diffing its Items would pin this row red forever and destroy its ability to signal when the two real divergences below are fixed; the projection is scoped to that leg alone and nothing was added to parity_diff.VOLATILE. Two causes remain, BOTH owned by other rows and deliberately left visible here rather than filtered out of the probe: (1) ITEM UNIVERSE — Ferrofin's recursive user-root universe has no MusicArtist, Folder, TvChannel or UserView rows (F 552 vs J 561 unfiltered; per type F 0 vs J 3/3/2/1), the same parentless-by-name/TopParentId cause as GET /Artists/{itemId}/Similar; detector `/Items?userId=<u>&recursive=true&limit=100000`, which shows the identical 9-row delta, so it is not this handler's bug. (2) PER-ITEM DTO FIELDS — MediaSources[].Name/Bitrate, MediaStreams[].BitRate (a constant 192000 offset), LocalizedLanguage, Level/IsAVC/Width/Height: the same set already classified on GET /Items/{itemId} as independent-probe noise, inherited here because Suggestions returns full item DTOs. Narrowing the compare to hide either would be the dishonest green; the row stays red until they are fixed on their own rows. The multi-leg probe no longer skips a leg whose servers disagree on status: a status divergence (or a body that is not 200-with-JSON on both sides) is recorded as a mismatch, which is the whole point of keeping the type=MusicArtist and type=TvChannel legs. UNIVERSE GAP, re-measured 2026-08-29 and WIDER than first written: LiveTvProgram belongs on the missing-kind list too — `/Items?userId=<u>&recursive=true&includeItemTypes=LiveTvProgram` returns 338 rows on Jellyfin and 0 on Ferrofin, while `/LiveTv/Programs` returns the guide on both (F 19200, J 338). The knock-on is visible on a THIRD row: GET /Genres is red only because Jellyfin's genre list carries `News`, the XMLTV guide's programme genre, which Ferrofin's genre list cannot see for the same reason. One fix (give the by-name/programme rows a TopParentId so `scope_to_user_libraries` stops filtering them out) closes /Artists Similar, these Suggestions legs and the /Genres row together; it changes the recursive item universe server-wide, so it is its own change WITH the perf gate.
- ⛏ `GET /Items/{itemId}/Ancestors` — open-work (NOT accepted): with both containers on the same data dir the ancestor rows now align by Path and every field matches except two, BOTH on the UserRootFolder hop. (1) ChildCount J=4 H=3 — UserRootFolder.GetEligibleChildrenForRecursiveChildren (UserRootFolder.cs:96-102) concatenates LibraryManager.RootFolder.VirtualChildren, which is the PlaylistsFolder CreateRootFolder adds at {data}/playlists (LibraryManager.cs:1090-1120); Ferrofin stores that folder parentless and has no AggregateFolder root with virtual children, so the user root counts 3 libraries instead of 4 and /Items?parentId=<root> returns 3. (2) PrimaryImageAspectRatio present on Jellyfin, absent on Ferrofin — FolderImageProvider generates a collage for the user root (Supports() rejects only PhotoAlbum/MusicAlbum and IsTopParent folders); Ferrofin ports CollectionFolderImageProvider (the library tiles) but not FolderImageProvider, so the user root has no Primary image. Un-defer path: port UserRootFolder's virtual-children concat (the direct-children query AND the ChildCount prefetch) and FolderImageProvider. The previous text on this row claimed the residual was '/config-vs-/data path-derived ids only'; that was measurably false — the ids were already identical on both servers and only the Path STRING differed, which is the array align key, so no field was ever compared.
- ⛏ `GET /LiveTv/TunerHosts/Types` — open-work (NOT accepted): Ferrofin advertises only the M3U tuner host; Jellyfin also lists HDHomeRun. Port of HdHomerunHost (~1.2K C# LOC: UDP discovery, lineup, legacy control protocol, HTTP/UDP streams) is an open work item — see brain/DEFERRED.md 2026-08-22 sweep.

## Flagged by a detector — UNREVIEWED (not accepted, not verified)

_A probe recorded a divergence and wrote its own text. Nobody has looked at these yet: they are neither an accepted divergence nor a named work item. They sat under "accepted — not a bug" until this section existed._

- 🔍 `GET /Artists` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Artists/AlbumArtists` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Artists/InstantMix` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Artists/{name}` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Devices` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /DisplayPreferences/{displayPreferencesId}` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Items` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Items/Counts` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Items/Filters` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Items/Filters2` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Items/Latest` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Items/Root` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Items/{itemId}/ExternalIdInfos` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Items/{itemId}/Images` — flagged: read diff vs Jellyfin (verify)
- 🔍 `DELETE /Items/{itemId}/Images/{imageType}` — flagged: asset write effect diff vs Jellyfin (verify)
- 🔍 `GET /Items/{itemId}/Images/{imageType}` — flagged: asset property diff vs Jellyfin (verify)
- 🔍 `GET /Items/{itemId}/Images/{imageType}/{imageIndex}` — flagged: asset property diff vs Jellyfin (verify)
- 🔍 `GET /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}` — flagged: asset property diff vs Jellyfin (verify)
- 🔍 `GET /Items/{itemId}/MetadataEditor` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Items/{itemId}/RemoteImages` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Items/{itemId}/RemoteImages/Providers` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Library/MediaFolders` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Library/PhysicalPaths` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Library/VirtualFolders` — flagged: read diff vs Jellyfin (verify)
- 🔍 `POST /Library/VirtualFolders/LibraryOptions` — flagged: Ferrofin read-back did not reflect the write (verify: real gap vs read-back method)
- 🔍 `GET /LiveTv/Channels` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /LiveTv/Channels/{channelId}` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /LiveTv/Info` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /LiveTv/Programs` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /LiveTv/Programs/Recommended` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /LiveTv/Recordings` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /LiveTv/Recordings/Folders` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Movies/Recommendations` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /MusicGenres` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /MusicGenres/{genreName}` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /Search/Hints` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /UserItems/{itemId}/UserData` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /UserViews` — flagged: read diff vs Jellyfin (verify)
- 🔍 `GET /UserViews/GroupingOptions` — flagged: read diff vs Jellyfin (sweep single-item align)
- 🔍 `GET /Videos/{itemId}/Trickplay/{width}/tiles.m3u8` — flagged: stream signature diff vs Jellyfin (verify)
- 🔍 `GET /Videos/{itemId}/Trickplay/{width}/{index}.jpg` — flagged: stream signature diff vs Jellyfin (verify)

## Full ledger

_status/schema: ✅ pass · ⚠️ fail · · untested_  

_verified: ✅ body-diff · ◐ property · ⊙ effect · ▫ status-class · ∅ empty-corpus · ⚠️ fail · · untested · ⛔ a verdict with no declared method (rejected before this file is written; it must never appear). Only ✅ is deep-verified._

| operation | route | depth | status | schema | verified | classification |
|---|---|---|---|---|---|---|
| `GET /Albums/{itemId}/InstantMix` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Albums/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ∅ | flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. GREEN BUT VACUOUS ON THE CURRENT FIXTURE — read this row as 'no divergence detected', not as 'verified'. `suite/perf/gen-fixtures.sh` gave each of the 3 albums a UNIQUE music genre, so the candidate universe is empty by construction on BOTH servers and the leg diffs `{"Items": [], "TotalRecordCount": 0}` against itself. The fixture is changed (two of the three artists now share a genre) but that only takes effect at the campaign's final wipe-and-provision; until that sweep runs, the album matching itself is covered by the `an_album_seed_matches_albums_sharing_a_genre_only` unit test (two albums sharing a genre, a same-genre track that must NOT come back because IncludeItemTypes is [MusicAlbum] alone, and the ExcludeArtistIds leg with a positive control). A real Ferrofin bug found behind this row IS fixed and verified live: album Genres/Studios/PremiereDate are inherited from the album's Audio children (C# AlbumMetadataService + MetadataService.UpdateGenres/UpdateStudios/UpdatePremiereDate); Ferrofin aggregated only album-artist and year, so every album had `Genres: []` and was unreachable by the Genres= query this endpoint issues. After the fix, F album Genres/PremiereDate/ProductionYear match J exactly (Jazz/Rock/Ambient, 2022-01-01T00:00:00.0000000Z, 2022). |
| `GET /Artists` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Artists/AlbumArtists` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Artists/InstantMix` | registered | REAL | ✅ | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Artists/{itemId}/InstantMix` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Artists/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ⚠️ | open-work (NOT accepted): Ferrofin returns 0 rows where Jellyfin returns 2. The scorer half is fixed — the seed's kind now selects the C# provider shape (`MusicArtistSimilarItemsProvider` is a flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [MusicArtist] }`, and an EMPTY Genres/Tags pair is not a predicate, so an untagged artist must match every other artist; covered by the `an_untagged_artist_seed_still_matches_every_other_artist` unit test). What still blocks it is the item-UNIVERSE divergence, not the similarity code: Ferrofin materializes MusicArtist as a parentless by-name row with a NULL TopParentId, and every user-scoped `InternalItemsQuery` is scoped to the user's views via `TopParentId IS NOT NULL` (item_repository.rs `scope_to_user_libraries`, the port of C# `AddUserToQuery`), so no artist is reachable by any query that does not name it by id. Detector, both servers: `/Items?userId=<u>&recursive=true&includeItemTypes=MusicArtist` -> F 0, J 3, while `/Artists?userId=<u>` returns all 3 on both, and adding `&ids=<artistId>` makes the same query return the row on Ferrofin. Same root cause as the MusicArtist/Folder/TvChannel/UserView rows missing from GET /Items/Suggestions. Un-defer path: give the MusicArtist by-name row a TopParentId (Jellyfin's artist rows are physical folder items under the music library, which is why they survive `AddUserToQuery`) — this changes the recursive item universe server-wide, so it needs its own change with the perf gate, not a ride-along. ORDERING, for the record: this endpoint's provider orders `Random` on both servers (MusicArtistSimilarItemsProvider.cs:50 / v10.11.8 LibraryController.cs:801) and Ferrofin now emits `Random` too, so when the universe bug is fixed the reads leg must stay pinned at a limit above the candidate pool — the ORDER is not gateable by any probe, since parity_diff aligns arrays by key. |
| `GET /Artists/{name}` | registered | REAL | ⚠️ | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Artists/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /Artists/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `DELETE /Audio/{itemId}/Lyrics` | registered | REAL | · | · | · |  |
| `GET /Audio/{itemId}/Lyrics` | registered | REAL | ✅ | · | · |  |
| `POST /Audio/{itemId}/Lyrics` | registered | REAL | · | · | · |  |
| `GET /Audio/{itemId}/RemoteSearch/Lyrics` | registered | REAL | ✅ | ✅ | · |  |
| `POST /Audio/{itemId}/RemoteSearch/Lyrics/{lyricId}` | registered | REAL | · | · | · |  |
| `GET /Audio/{itemId}/hls/{segmentId}/stream.aac` | registered | REAL | · | · | ▫ |  |
| `GET /Audio/{itemId}/hls/{segmentId}/stream.mp3` | registered | REAL | · | · | ▫ |  |
| `GET /Audio/{itemId}/hls1/{playlistId}/{segmentId}.{container}` | registered | REAL | · | · | ◐ |  |
| `GET /Audio/{itemId}/main.m3u8` | registered | REAL | ✅ | · | ◐ |  |
| `GET /Audio/{itemId}/master.m3u8` | registered | REAL | ✅ | · | ◐ |  |
| `HEAD /Audio/{itemId}/master.m3u8` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Audio/{itemId}/stream` | registered | REAL | ✅ | · | ✅ |  |
| `HEAD /Audio/{itemId}/stream` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Audio/{itemId}/stream.{container}` | registered | REAL | ✅ | · | ✅ |  |
| `HEAD /Audio/{itemId}/stream.{container}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Audio/{itemId}/universal` | registered | REAL | ✅ | · | ✅ |  |
| `HEAD /Audio/{itemId}/universal` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Auth/Keys` | registered | REAL | ✅ | ✅ | ∅ |  |
| `POST /Auth/Keys` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Auth/Keys/{key}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Auth/PasswordResetProviders` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Auth/Providers` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Backup` | registered | REAL | ✅ | ✅ | ⊙ | ok |
| `POST /Backup/Create` | registered | REAL | · | · | ⊙ | ok |
| `GET /Backup/Manifest` | registered | REAL | ✅ | · | ⊙ | ok |
| `POST /Backup/Restore` | registered | REAL | · | · | ⊙ | ok |
| `GET /Branding/Configuration` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Branding/Css` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Branding/Css.css` | registered | REAL | ✅ | · | ▫ |  |
| `DELETE /Branding/Splashscreen` | registered | REAL | · | · | ⊙ |  |
| `GET /Branding/Splashscreen` | registered | REAL | ⚠️ | · | ⊙ |  |
| `POST /Branding/Splashscreen` | registered | REAL | · | · | ⊙ |  |
| `GET /Channels` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Channels/Features` | registered | REAL | ✅ | ✅ | · |  |
| `GET /Channels/Items/Latest` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Channels/{channelId}/Features` | registered | REAL | · | · | · | requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers. |
| `GET /Channels/{channelId}/Items` | registered | REAL | · | · | · | requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers. |
| `POST /ClientLog/Document` | registered | REAL | · | · | ⊙ | ok |
| `POST /Collections` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Collections/{collectionId}/Items` | registered | REAL | · | · | ⊙ | ok |
| `POST /Collections/{collectionId}/Items` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Devices` | registered | REAL | · | · | ⊙ | ok |
| `GET /Devices` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Devices/Info` | registered | REAL | ✅ | · | ✅ | ok |
| `GET /Devices/Options` | registered | REAL | ✅ | · | ⊙ | ok |
| `POST /Devices/Options` | registered | REAL | · | · | ⊙ | ok |
| `GET /DisplayPreferences/{displayPreferencesId}` | registered | REAL | · | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `POST /DisplayPreferences/{displayPreferencesId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Environment/DefaultDirectoryBrowser` | registered | REAL | ✅ | ✅ | · |  |
| `GET /Environment/DirectoryContents` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /Environment/Drives` | registered | REAL | ✅ | ✅ | ⚠️ | instance: per-host filesystem drives. |
| `GET /Environment/NetworkShares` | registered | REAL | ✅ | ✅ | · |  |
| `GET /Environment/ParentPath` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `POST /Environment/ValidatePath` | registered | REAL | · | · | ▫ | ok |
| `GET /Episode/{Id}/Timestamps` | registered | REAL | ⚠️ | ✅ | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `POST /Episode/{Id}/Timestamps` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Episode/{id}/IntroSkipperSegments` | registered | REAL | ⚠️ | ✅ | · | expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming. |
| `GET /FallbackFont/Fonts` | registered | REAL | ✅ | ✅ | · |  |
| `GET /FallbackFont/Fonts/{name}` | registered | REAL | ✅ | · | ▫ |  |
| `POST /FileTransformation/RegisterTransformation` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Genres` | registered | REAL | ✅ | ✅ | ⚠️ | verified 2026-08-11: QueryResult<BaseItemDto> (Items array + TotalRecordCount + StartIndex), status+schema conformant; empty-library returns the well-formed empty result. |
| `GET /Genres/{genreName}` | registered | REAL | ✅ | ✅ | ⚠️ | accepted-divergence: same no-lazy-create-on-GET family as /Studios/{name}. Existing genre→200 BaseItemDto (both servers), absent→Ferrofin 404 vs Jellyfin auto-created 200. Shape verified. |
| `GET /Genres/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ◐ |  |
| `HEAD /Genres/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Genres/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ◐ |  |
| `HEAD /Genres/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /GetUtcTime` | registered | REAL | ✅ | ✅ | · |  |
| `GET /IntroSkipper` | registered | REAL | ⚠️ | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /IntroSkipper/SupportBundle` | registered | REAL | ⚠️ | ⚠️ | · | expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (H=200 vs J=404). By design. |
| `POST /Intros/AnalyzerActions/UpdateSeason` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Intros/AnalyzerActions/{SeasonId}` | registered | REAL | ⚠️ | ✅ | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `POST /Intros/EraseTimestamps` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `POST /Intros/RebuildDatabase` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `POST /Intros/ScanSeason/{SeriesId}/{SeasonId}` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Intros/ScanStatus` | registered | REAL | ⚠️ | ✅ | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `DELETE /Intros/Show/{SeriesId}/{SeasonId}` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Intros/Show/{SeriesId}/{SeasonId}` | registered | REAL | ⚠️ | ✅ | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `DELETE /Items` | registered | REAL | · | · | ▫ | ok |
| `GET /Items` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Items/Counts` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Items/Filters` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Items/Filters2` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Items/Latest` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `POST /Items/RemoteSearch/Apply/{itemId}` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/Book` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/BoxSet` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/Movie` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/MusicAlbum` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/MusicArtist` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/MusicVideo` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/Person` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/Series` | registered | REAL | · | · | · |  |
| `POST /Items/RemoteSearch/Trailer` | registered | REAL | · | · | · |  |
| `GET /Items/Root` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Items/Suggestions` | registered | REAL | ✅ | ✅ | ⚠️ | open-work (NOT accepted). The old text ('Random, so the set is non-deterministic') was only half true and it hid two real divergences. Random here PERMUTES but does not change the set: `SuggestionsController` runs ONE query (`OrderBy = [(Random, Descending)]`, Recursive, mediaType/type, IsVirtualItem=false) and Ferrofin's handler is a line-for-line port, so at `limit >= |universe|` the answer is fully determined and parity_diff aligns Items by Path. The probe was the problem: `limit=10` sampled 10 of 552 at random and could never diff. It is now eight legs: seven pinned at a full limit (per type), where Random only permutes and the whole body IS diffed, plus one narrow startIndex/limit leg. That narrow leg is the one place a projection is applied — a page of 3 out of 500 Random-ordered rows draws a different triple per call on BOTH servers (measured: three consecutive calls, three different triples each side), so it compares StartIndex, TotalRecordCount and the page length only. Diffing its Items would pin this row red forever and destroy its ability to signal when the two real divergences below are fixed; the projection is scoped to that leg alone and nothing was added to parity_diff.VOLATILE. Two causes remain, BOTH owned by other rows and deliberately left visible here rather than filtered out of the probe: (1) ITEM UNIVERSE — Ferrofin's recursive user-root universe has no MusicArtist, Folder, TvChannel or UserView rows (F 552 vs J 561 unfiltered; per type F 0 vs J 3/3/2/1), the same parentless-by-name/TopParentId cause as GET /Artists/{itemId}/Similar; detector `/Items?userId=<u>&recursive=true&limit=100000`, which shows the identical 9-row delta, so it is not this handler's bug. (2) PER-ITEM DTO FIELDS — MediaSources[].Name/Bitrate, MediaStreams[].BitRate (a constant 192000 offset), LocalizedLanguage, Level/IsAVC/Width/Height: the same set already classified on GET /Items/{itemId} as independent-probe noise, inherited here because Suggestions returns full item DTOs. Narrowing the compare to hide either would be the dishonest green; the row stays red until they are fixed on their own rows. The multi-leg probe no longer skips a leg whose servers disagree on status: a status divergence (or a body that is not 200-with-JSON on both sides) is recorded as a mismatch, which is the whole point of keeping the type=MusicArtist and type=TvChannel legs. UNIVERSE GAP, re-measured 2026-08-29 and WIDER than first written: LiveTvProgram belongs on the missing-kind list too — `/Items?userId=<u>&recursive=true&includeItemTypes=LiveTvProgram` returns 338 rows on Jellyfin and 0 on Ferrofin, while `/LiveTv/Programs` returns the guide on both (F 19200, J 338). The knock-on is visible on a THIRD row: GET /Genres is red only because Jellyfin's genre list carries `News`, the XMLTV guide's programme genre, which Ferrofin's genre list cannot see for the same reason. One fix (give the by-name/programme rows a TopParentId so `scope_to_user_libraries` stops filtering them out) closes /Artists Similar, these Suggestions legs and the /Genres row together; it changes the recursive item universe server-wide, so it is its own change WITH the perf gate. |
| `DELETE /Items/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Items/{itemId}` | registered | REAL | ✅ | ✅ | ⚠️ | jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. NOTE: the previous 'instance/probe-noise' text on this row was DISPROVEN in batch A3 — running both containers' ffprobe binaries over the same file gives byte-identical JSON (7.1.4 vs 7.1.3-Jellyfin), and ffprobe emits no per-stream bit_rate at all. The five other residuals it covered were real server bugs and are fixed: MediaSources[].Name now ports BaseItem.GetMediaSourceName (file stem, not item.Name); codec_tag_string binds the correct 10.11.8 JSON key (the trailing-'?' typo is master-only); Level/IsAVC are set on every stream and subtitle Width/Height default to 0, matching C#'s non-nullable probing DTO; LocalizedLanguage is no longer serialized (off-contract, the cause of schema_valid:false); and the People[].Role read projection no longer NULLIFs the empty string C# always emits. |
| `POST /Items/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Items/{itemId}/Ancestors` | registered | REAL | ✅ | ✅ | ⚠️ | open-work (NOT accepted): with both containers on the same data dir the ancestor rows now align by Path and every field matches except two, BOTH on the UserRootFolder hop. (1) ChildCount J=4 H=3 — UserRootFolder.GetEligibleChildrenForRecursiveChildren (UserRootFolder.cs:96-102) concatenates LibraryManager.RootFolder.VirtualChildren, which is the PlaylistsFolder CreateRootFolder adds at {data}/playlists (LibraryManager.cs:1090-1120); Ferrofin stores that folder parentless and has no AggregateFolder root with virtual children, so the user root counts 3 libraries instead of 4 and /Items?parentId=<root> returns 3. (2) PrimaryImageAspectRatio present on Jellyfin, absent on Ferrofin — FolderImageProvider generates a collage for the user root (Supports() rejects only PhotoAlbum/MusicAlbum and IsTopParent folders); Ferrofin ports CollectionFolderImageProvider (the library tiles) but not FolderImageProvider, so the user root has no Primary image. Un-defer path: port UserRootFolder's virtual-children concat (the direct-children query AND the ChildCount prefetch) and FolderImageProvider. The previous text on this row claimed the residual was '/config-vs-/data path-derived ids only'; that was measurably false — the ids were already identical on both servers and only the Path STRING differed, which is the array align key, so no field was ever compared. |
| `POST /Items/{itemId}/ContentType` | registered | REAL | · | · | ▫ | ok |
| `GET /Items/{itemId}/CriticReviews` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Items/{itemId}/Download` | registered | REAL | ✅ | · | ✅ |  |
| `GET /Items/{itemId}/ExternalIdInfos` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Items/{itemId}/File` | registered | REAL | ✅ | · | ✅ |  |
| `GET /Items/{itemId}/Images` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `DELETE /Items/{itemId}/Images/{imageType}` | registered | REAL | · | · | ⚠️ | flagged: asset write effect diff vs Jellyfin (verify) |
| `GET /Items/{itemId}/Images/{imageType}` | registered | REAL | ✅ | · | ⚠️ | flagged: asset property diff vs Jellyfin (verify) |
| `HEAD /Items/{itemId}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `POST /Items/{itemId}/Images/{imageType}` | registered | REAL | · | · | ⊙ |  |
| `DELETE /Items/{itemId}/Images/{imageType}/{imageIndex}` | registered | REAL | · | · | ⊙ |  |
| `GET /Items/{itemId}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ⚠️ | flagged: asset property diff vs Jellyfin (verify) |
| `HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `POST /Items/{itemId}/Images/{imageType}/{imageIndex}` | registered | REAL | · | · | ⊙ |  |
| `POST /Items/{itemId}/Images/{imageType}/{imageIndex}/Index` | registered | REAL | · | · | ⊙ |  |
| `GET /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}` | registered | REAL | ✅ | · | ⚠️ | flagged: asset property diff vs Jellyfin (verify) |
| `HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Items/{itemId}/InstantMix` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Items/{itemId}/Intros` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Items/{itemId}/LocalTrailers` | registered | REAL | ✅ | ✅ | · |  |
| `GET /Items/{itemId}/MetadataEditor` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Items/{itemId}/PlaybackInfo` | registered | REAL | ✅ | ✅ | ⚠️ | jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. Same root cause and same batch-A3 fixes as GET /Items/{itemId}; the 'instance/probe-noise' text previously on this row was equally disproven. |
| `POST /Items/{itemId}/PlaybackInfo` | registered | REAL | · | · | ⊙ | ok |
| `POST /Items/{itemId}/Refresh` | registered | REAL | · | · | ▫ | ok |
| `GET /Items/{itemId}/RemoteImages` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `POST /Items/{itemId}/RemoteImages/Download` | registered | REAL | · | · | · |  |
| `GET /Items/{itemId}/RemoteImages/Providers` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Items/{itemId}/RemoteSearch/Subtitles/{language}` | registered | REAL | ✅ | ✅ | · |  |
| `POST /Items/{itemId}/RemoteSearch/Subtitles/{subtitleId}` | registered | REAL | · | · | · | remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live. |
| `GET /Items/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ⚠️ | instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93). |
| `GET /Items/{itemId}/SpecialFeatures` | registered | REAL | ✅ | ✅ | · |  |
| `GET /Items/{itemId}/ThemeMedia` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Items/{itemId}/ThemeSongs` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Items/{itemId}/ThemeVideos` | registered | REAL | ✅ | ✅ | ∅ |  |
| `POST /Jellyfin.Plugin.OpenSubtitles/ValidateLoginInfo` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Libraries/AvailableOptions` | registered | REAL | ✅ | ✅ | ⚠️ | expected-extension (SupportedImageTypes + the fetcher lists): Ferrofin compiles in TheTVDB, FanArt, Open Subtitles and IntroSkipper, which the lab's stock Jellyfin 10.11.8 does not ship (GET /Plugins there lists only AudioDB and MusicBrainz). They therefore appear as extra ImageFetchers/MetadataFetchers rows, and — because SupportedImageTypes is the flattened union of every compiled image provider's GetSupportedImages (ProviderManager.cs:706-714) — their image types (Art/Disc/Banner) widen that list beyond Jellyfin's. The 2026-08-11 'verified' text this replaces was false: sweep already recorded 15 extra / 39 mismatch / 2 missing paths and schema_valid:false on this row. Fixed in batch A3: DefaultImageOptions is now the verbatim TypeOptions.DefaultImageOptions table (order included, [] for unnamed types); SupportedImageTypes is derived from the providers instead of a hardcoded 11-element enum dump; isNewLibrary now drives DefaultEnabled; and SimilarItemProviders (post-10.11.8, additionalProperties:false) is no longer serialized. OPEN WORK, not accepted: upstream's 'Screen Grabber' (VideoImageProvider, Order 100, [Primary]) is still unported — Ferrofin owns the capability (MediaEncoder::extract_video_image) but does not register the provider, so it is deliberately absent from the list rather than advertised hollow. |
| `POST /Library/Media/Updated` | registered | REAL | · | · | · |  |
| `GET /Library/MediaFolders` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `POST /Library/Movies/Added` | registered | REAL | · | · | · |  |
| `POST /Library/Movies/Updated` | registered | REAL | · | · | · |  |
| `GET /Library/PhysicalPaths` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `POST /Library/Refresh` | registered | REAL | · | · | ▫ | ok |
| `POST /Library/Series/Added` | registered | REAL | · | · | · |  |
| `POST /Library/Series/Updated` | registered | REAL | · | · | · |  |
| `DELETE /Library/VirtualFolders` | registered | REAL | · | · | ⊙ | ok |
| `GET /Library/VirtualFolders` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `POST /Library/VirtualFolders` | registered | REAL | · | · | ⊙ | ok |
| `POST /Library/VirtualFolders/LibraryOptions` | registered | REAL | · | · | ⚠️ | flagged: Ferrofin read-back did not reflect the write (verify: real gap vs read-back method) |
| `POST /Library/VirtualFolders/Name` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Library/VirtualFolders/Paths` | registered | REAL | · | · | ⊙ | ok |
| `POST /Library/VirtualFolders/Paths` | registered | REAL | · | · | ⊙ | ok |
| `POST /Library/VirtualFolders/Paths/Update` | registered | REAL | · | · | ⊙ | ok |
| `POST /LiveStreams/Close` | registered | REAL | · | · | ⊙ | ok |
| `POST /LiveStreams/Open` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/ChannelMappingOptions` | registered | REAL | ⚠️ | ✅ | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider. |
| `POST /LiveTv/ChannelMappings` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider. |
| `GET /LiveTv/Channels` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /LiveTv/Channels/{channelId}` | registered | REAL | · | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /LiveTv/GuideInfo` | registered | REAL | ✅ | ✅ | ⚠️ | instance: now-relative guide window (now .. now+7d) matches Jellyfin's behavior, but the exact StartDate/EndDate timestamps can't match across two independently-clocked servers. |
| `GET /LiveTv/Info` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `DELETE /LiveTv/ListingProviders` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture's listings provider and observes the guide emptying. |
| `POST /LiveTv/ListingProviders` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/ListingProviders/Default` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /LiveTv/ListingProviders/Lineups` | registered | REAL | ⚠️ | ✅ | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a lineup probe (needs a Schedules Direct-style provider; xmltv has none). |
| `GET /LiveTv/ListingProviders/SchedulesDirect/Countries` | registered | REAL | ✅ | ⚠️ | ✅ |  |
| `GET /LiveTv/LiveRecordings/{recordingId}/stream` | registered | REAL | · | · | ◐ | ok |
| `GET /LiveTv/LiveStreamFiles/{streamId}/stream.{container}` | registered | REAL | ✅ | · | ◐ | ok |
| `GET /LiveTv/Programs` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `POST /LiveTv/Programs` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a programmes-by-body probe (the POST form of /LiveTv/Programs). |
| `GET /LiveTv/Programs/Recommended` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /LiveTv/Programs/{programId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/Recordings` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /LiveTv/Recordings/Folders` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /LiveTv/Recordings/Groups` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /LiveTv/Recordings/Groups/{groupId}` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a recordings-group probe once a recording exists. |
| `GET /LiveTv/Recordings/Series` | registered | REAL | ✅ | ✅ | ∅ |  |
| `DELETE /LiveTv/Recordings/{recordingId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/Recordings/{recordingId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/SeriesTimers` | registered | REAL | ✅ | ✅ | ∅ |  |
| `POST /LiveTv/SeriesTimers` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete). |
| `DELETE /LiveTv/SeriesTimers/{timerId}` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete). |
| `GET /LiveTv/SeriesTimers/{timerId}` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete). |
| `POST /LiveTv/SeriesTimers/{timerId}` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete). |
| `GET /LiveTv/Timers` | registered | REAL | ✅ | ✅ | ∅ |  |
| `POST /LiveTv/Timers` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/Timers/Defaults` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `DELETE /LiveTv/Timers/{timerId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/Timers/{timerId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /LiveTv/Timers/{timerId}` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs an update step in the timer journey (change pre-padding, read back). |
| `DELETE /LiveTv/TunerHosts` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture tuner and observes its channels going. |
| `POST /LiveTv/TunerHosts` | registered | REAL | · | · | ⊙ | ok |
| `GET /LiveTv/TunerHosts/Types` | registered | REAL | ✅ | ✅ | ⚠️ | open-work (NOT accepted): Ferrofin advertises only the M3U tuner host; Jellyfin also lists HDHomeRun. Port of HdHomerunHost (~1.2K C# LOC: UDP discovery, lineup, legacy control protocol, HTTP/UDP streams) is an open work item — see brain/DEFERRED.md 2026-08-22 sweep. |
| `GET /LiveTv/Tuners/Discover` | registered | REAL | ✅ | ✅ | · |  |
| `GET /LiveTv/Tuners/Discvover` | registered | REAL | ✅ | ✅ | · |  |
| `POST /LiveTv/Tuners/{tunerId}/Reset` | registered | REAL | · | · | · | requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a tuner-reset probe with the fixture tuner's id. |
| `GET /Localization/Countries` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Localization/Cultures` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /Localization/Options` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Localization/ParentalRatings` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /MediaSegments/{itemId}` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /MediaSegmentsApi` | registered | REAL | ⚠️ | · | · | expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming. |
| `POST /MediaSegmentsApi/{itemId}` | registered | REAL | · | · | · | expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming. |
| `DELETE /MediaSegmentsApi/{segmentId}` | registered | REAL | · | · | · | expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming. |
| `POST /MergeVersions/MergeEpisodes` | registered | REAL | · | · | ⚠️ | expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design. |
| `POST /MergeVersions/MergeMovies` | registered | REAL | · | · | ⚠️ | expected-extension: Ferrofin ships the MergeVersions controller (danieladov 'Merge Versions' plugin surface) compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design. |
| `POST /MergeVersions/SplitEpisodes` | registered | REAL | · | · | ⚠️ | expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design. |
| `POST /MergeVersions/SplitMovies` | registered | REAL | · | · | ⚠️ | expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design. |
| `GET /Movies/Recommendations` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Movies/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ⚠️ | instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93). |
| `GET /MusicGenres` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /MusicGenres/InstantMix` | registered | REAL | ✅ | · | · |  |
| `GET /MusicGenres/{genreName}` | registered | REAL | ⚠️ | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /MusicGenres/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /MusicGenres/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /MusicGenres/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /MusicGenres/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /MusicGenres/{name}/InstantMix` | registered | REAL | ✅ | ✅ | ∅ |  |
| `GET /Packages` | registered | REAL | ✅ | ✅ | ⚠️ | instance: aggregates the enabled repositories' manifests (PackageInfo[]) plus synthesized compiled-in entries; repositoryName/Url stamped from the repository actually fetched (manifest claims are untrusted); manifest fetch is size-capped + timed out. Catalog content is repository-config-specific — harness instances configure none — so nothing to diff. Verified 2026-08-14 by unit + server HTTP tests. |
| `POST /Packages/Installed/{name}` | registered | REAL | · | · | · | accepted-divergence: repository install of WASM plugins is real (2026-08-14: ABI gate → HTTPS-per-hop download → streamed size cap → sha256/md5 verify → component+identity validation → guid-squat refusal → atomic stage → restart-required; admin-only; end-to-end server test). Divergence vs Jellyfin: the install is SYNCHRONOUS — none of the PackageInstalling/Completed/Failed/Cancelled WebSocket messages are emitted (no dashboard progress; a slow repo holds the POST open), and the plugin appears in /Plugins only after the activating restart. Async install + Package* push is the recorded follow-up (WebSocket-push backlog). |
| `DELETE /Packages/Installing/{packageId}` | registered | REAL | · | · | · | accepted-divergence: always 404 — installs are synchronous, so no tracked installation exists to cancel (Jellyfin tracks async installs). Admin-only. |
| `GET /Packages/{name}` | registered | REAL | ✅ | · | · | instance: resolves a package by name/assemblyGuid from the aggregated repository catalog — catalog content is repository-config-specific, and the harness instances configure none, so there is no package to diff. Route verified 2026-08-14 by unit + server HTTP tests. |
| `GET /Persons` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /Persons/{name}` | registered | REAL | ✅ | · | ⚠️ | accepted-divergence: same no-lazy-create-on-GET family as /Studios/{name}. Existing person→200 BaseItemDto, absent→404 (both servers 404 in the harness run). Shape verified. |
| `GET /Persons/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /Persons/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Persons/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /Persons/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Playback/BitrateTest` | registered | REAL | ✅ | · | ◐ |  |
| `DELETE /PlayingItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /PlayingItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /PlayingItems/{itemId}/Progress` | registered | REAL | · | · | ⊙ | ok |
| `POST /Playlists` | registered | REAL | · | · | ⊙ | ok |
| `GET /Playlists/{itemId}/InstantMix` | registered | REAL | ⚠️ | ✅ | · | expected-extension: Ferrofin generates an instant mix for a playlist (H=200); stock Jellyfin returns 404 (playlists are not an instant-mix source). A harmless superset. |
| `GET /Playlists/{playlistId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /Playlists/{playlistId}` | registered | REAL | · | · | ▫ | ok |
| `DELETE /Playlists/{playlistId}/Items` | registered | REAL | · | · | ⊙ | ok |
| `GET /Playlists/{playlistId}/Items` | registered | REAL | · | · | ⊙ | ok |
| `POST /Playlists/{playlistId}/Items` | registered | REAL | · | · | ⊙ | ok |
| `POST /Playlists/{playlistId}/Items/{itemId}/Move/{newIndex}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Playlists/{playlistId}/Users` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Playlists/{playlistId}/Users/{userId}` | registered | REAL | · | · | ⚠️ | instance/methodology: Ferrofin correctly removes the playlist share (H read-back shows it gone); Jellyfin returns 204 but still lists the user (Jellyfin quirk), so Ferrofin is the more-correct side — not a defect. |
| `GET /Playlists/{playlistId}/Users/{userId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /Playlists/{playlistId}/Users/{userId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Plugins` | registered | REAL | ✅ | ✅ | ⚠️ | expected-extension: lists Ferrofin's installed plugins (compiled-in extensions + staged WASM plugins) — the set necessarily differs from any stock Jellyfin instance. Route is real and schema-conformant (server HTTP tests). |
| `DELETE /Plugins/{pluginId}` | registered | REAL | · | · | · | accepted-divergence: real uninstall for a staged WASM plugin (file + config + enabled override removed, restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only. |
| `GET /Plugins/{pluginId}/Configuration` | registered | REAL | · | · | · | instance: the two servers share no plugin id (Ferrofin ships compiled-in extensions + WASM plugins; stock Jellyfin ships neither), so there is no config to fetch on both sides. Real read (stored values overlaid on defaults, C# partial-deserialize semantics); verified 2026-08-14 by unit + handler tests. |
| `POST /Plugins/{pluginId}/Configuration` | registered | REAL | · | · | · | instance: no shared plugin id across the two servers, so no config write to diff. Real write (JSON-validated, persisted; admin-only — a WASM plugin's config is guest input); verified 2026-08-14 by unit + handler tests incl. 403. |
| `POST /Plugins/{pluginId}/Manifest` | registered | REAL | · | · | · | instance: no shared plugin id across the two servers, so no manifest to fetch on both sides. Real read (GetPluginManifest port); verified 2026-08-14 by handler tests. |
| `DELETE /Plugins/{pluginId}/{version}` | registered | REAL | · | · | · | accepted-divergence: real uninstall for a staged WASM plugin (restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only. |
| `POST /Plugins/{pluginId}/{version}/Disable` | registered | REAL | · | · | · | accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window. |
| `POST /Plugins/{pluginId}/{version}/Enable` | registered | REAL | · | · | · | accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window. |
| `GET /Plugins/{pluginId}/{version}/Image` | registered | REAL | · | · | · | instance: no shared plugin id across the two servers, so no image to fetch on both sides. Real read (bundled image bytes + content type); verified 2026-08-14 by handler tests. |
| `GET /Providers/Lyrics/{lyricId}` | registered | REAL | · | · | · |  |
| `GET /Providers/Subtitles/Subtitles/{subtitleId}` | registered | REAL | · | · | · | remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live. |
| `POST /QuickConnect/Authorize` | registered | REAL | · | · | ▫ | ok |
| `GET /QuickConnect/Connect` | registered | REAL | ✅ | · | ⊙ | ok |
| `GET /QuickConnect/Enabled` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /QuickConnect/Initiate` | registered | REAL | · | · | ⊙ | ok |
| `GET /Repositories` | registered | REAL | ✅ | ✅ | ⚠️ | instance: returns the admin-configured package repositories — instance-local state (harness instances configure none), nothing to diff. Verified 2026-08-14 by unit + handler tests. |
| `POST /Repositories` | registered | REAL | · | · | · | instance: replaces the configured package repositories (admin-only, RequiresElevation port) — instance-local state, nothing to diff. Verified 2026-08-14 by unit + handler tests incl. 403 for non-admin. |
| `GET /ScheduledTasks` | registered | REAL | ✅ | ✅ | ⚠️ | instance: LastExecutionResult depends on whether/when a task last ran. |
| `DELETE /ScheduledTasks/Running/{taskId}` | registered | REAL | · | · | ▫ | ok |
| `POST /ScheduledTasks/Running/{taskId}` | registered | REAL | · | · | ▫ | ok |
| `GET /ScheduledTasks/{taskId}` | registered | REAL | · | · | ✅ | ok |
| `POST /ScheduledTasks/{taskId}/Triggers` | registered | REAL | · | · | ⊙ | ok |
| `GET /Search/Hints` | registered | REAL | ✅ | · | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /Sessions` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `POST /Sessions/Capabilities` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Capabilities/Full` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Logout` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Playing` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Playing/Ping` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/Playing/Progress` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Playing/Stopped` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/Viewing` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/Command` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/Command/{command}` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/Message` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/Playing` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/Playing/{command}` | registered | REAL | · | · | ▫ | ok |
| `POST /Sessions/{sessionId}/System/{command}` | registered | REAL | · | · | ▫ | ok |
| `DELETE /Sessions/{sessionId}/User/{userId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/{sessionId}/User/{userId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /Sessions/{sessionId}/Viewing` | registered | REAL | · | · | ▫ | ok |
| `GET /Shows/NextUp` | registered | REAL | ✅ | ✅ | ∅ | ok |
| `GET /Shows/Upcoming` | registered | REAL | ✅ | ✅ | ∅ | ok |
| `GET /Shows/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ✅ | flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. SERIES SEED: the pre-A2 server ran the weighted MOVIE scorer for every kind, which credits a shared studio (+5) and shared people and pulled genre-unrelated series in (F ['Series 06','Series 03','Series 05','Series 07'] vs J ['Series 06']); `SeriesSimilarItemsProvider` filters on Genres/Tags alone, and porting it made the set match. The probe also exposed a DTO gap since fixed: Ferrofin omitted `AirDays` on Series where 10.11.8 always sends `[]` (v10.11.8 DtoService.cs:1243 assigns `series.AirDays`, and v10.11.8 Series.cs:31 initialises it to `Array.Empty<DayOfWeek>()` while no column persists it) — the null-where-Jellyfin-sends-non-null shape strict clients crash on. |
| `GET /Shows/{seriesId}/Episodes` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /Shows/{seriesId}/Seasons` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `POST /SkipButtonCss/InjectCss` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `POST /SkipButtonCss/UpdateSkipDuration` | registered | REAL | · | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /Songs/{itemId}/InstantMix` | registered | REAL | ✅ | ✅ | ∅ |  |
| `POST /Startup/Complete` | registered | REAL | · | · | ⊙ | ok |
| `GET /Startup/Configuration` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /Startup/Configuration` | registered | REAL | · | · | ⊙ | ok |
| `GET /Startup/FirstUser` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /Startup/RemoteAccess` | registered | REAL | · | · | ⊙ | ok |
| `GET /Startup/User` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /Startup/User` | registered | REAL | · | · | ⚠️ | accepted-divergence: post-setup the first user already has a password and the contract is 403 (the Forbid guard upstream added in v12, ported by Ferrofin); Jellyfin 10.11.8 predates it and answers 204, silently re-setting the admin password. The journey asserts the correct contract (H=403 J=204). |
| `GET /Studios` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /Studios/{name}` | registered | REAL | ⚠️ | · | ⚠️ | accepted-divergence: Ferrofin does not lazily CREATE a by-name item on a GET (Jellyfin's GetItemByName writes a row as a read side-effect → J=200 for an absent studio). Ferrofin returns the item if it exists (200, BaseItemDto) else 404 — a GET with no filesystem write. Verified live 2026-08-11: existing→200, absent→404. |
| `GET /Studios/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /Studios/{name}/Images/{imageType}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Studios/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /Studios/{name}/Images/{imageType}/{imageIndex}` | registered | REAL | ✅ | · | ▫ |  |
| `POST /SyncPlay/Buffering` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Join` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Leave` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `GET /SyncPlay/List` | registered | REAL | ✅ | ✅ | · |  |
| `POST /SyncPlay/MovePlaylistItem` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/New` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/NextItem` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Pause` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Ping` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/PreviousItem` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Queue` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Ready` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/RemoveFromPlaylist` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Seek` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/SetIgnoreWait` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/SetNewQueue` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/SetPlaylistItem` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/SetRepeatMode` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/SetShuffleMode` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Stop` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `POST /SyncPlay/Unpause` | registered | REAL | · | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `GET /SyncPlay/{id}` | registered | REAL | ✅ | · | · | verified-by-push-probe: the response body carries no signal here — the observable behaviour is the SyncPlayCommand/SyncPlayGroupUpdate pushed to member sockets. Verified end-to-end over two real WebSockets by `suite/run.sh push` (suite/ws/probe_remote_control.py). |
| `GET /System/ActivityLog/Entries` | registered | REAL | ✅ | ✅ | ⚠️ | instance: activity-log entries differ per instance/run. |
| `GET /System/Configuration` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /System/Configuration` | registered | REAL | · | · | ⊙ | ok |
| `POST /System/Configuration/Branding` | registered | REAL | · | · | ⊙ | ok |
| `GET /System/Configuration/MetadataOptions/Default` | registered | REAL | ✅ | ✅ | · |  |
| `GET /System/Configuration/{key}` | registered | REAL | · | · | ⊙ | ok |
| `POST /System/Configuration/{key}` | registered | REAL | · | · | ⊙ | ok |
| `GET /System/Endpoint` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /System/Info` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `GET /System/Info/Public` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /System/Info/Storage` | registered | REAL | ✅ | ✅ | ⚠️ | instance: per-host disk paths and free/used space. |
| `GET /System/Logs` | registered | REAL | ✅ | ✅ | ⚠️ | instance: log-file set differs per instance/run. |
| `GET /System/Logs/Log` | registered | REAL | ✅ | · | ◐ |  |
| `GET /System/Ping` | registered | REAL | ✅ | ✅ | ⚠️ | instance: returns the server name string (ferrofin vs the Jellyfin instance name). |
| `POST /System/Ping` | registered | REAL | · | · | ▫ | ok |
| `POST /System/Restart` | registered | REAL | · | · | ⊙ | ok |
| `POST /System/Shutdown` | registered | REAL | · | · | ⊙ | ok |
| `GET /Tmdb/ClientConfiguration` | registered | REAL | ✅ | ✅ | ✅ |  |
| `GET /Trailers` | registered | REAL | ⚠️ | ✅ | · | instance: Jellyfin 10.11.8 returns 500 here (its own error with no Trailers provider), while Ferrofin correctly returns 200 with an empty result. Kept correct per policy (do not port Jellyfin bugs); not a Ferrofin gap. |
| `GET /Trailers/{itemId}/Similar` | registered | REAL | ✅ | ✅ | ⚠️ | instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93). |
| `DELETE /UserFavoriteItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /UserFavoriteItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /UserImage` | registered | REAL | · | · | ⊙ |  |
| `GET /UserImage` | registered | REAL | ✅ | · | ▫ |  |
| `HEAD /UserImage` | registered | REAL | ✅ | · | ▫ |  |
| `POST /UserImage` | registered | REAL | · | · | ⊙ |  |
| `GET /UserItems/Resume` | registered | REAL | ✅ | ✅ | ∅ | ok |
| `DELETE /UserItems/{itemId}/Rating` | registered | REAL | · | · | ⊙ | ok |
| `POST /UserItems/{itemId}/Rating` | registered | REAL | · | · | ⊙ | ok |
| `GET /UserItems/{itemId}/UserData` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `POST /UserItems/{itemId}/UserData` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /UserPlayedItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `POST /UserPlayedItems/{itemId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /UserViews` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (verify) |
| `GET /UserViews/GroupingOptions` | registered | REAL | ✅ | ✅ | ⚠️ | flagged: read diff vs Jellyfin (sweep single-item align) |
| `GET /Users` | registered | REAL | ✅ | ✅ | ✅ |  |
| `POST /Users` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/AuthenticateByName` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/AuthenticateWithQuickConnect` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/Configuration` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/ForgotPassword` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/ForgotPassword/Pin` | registered | REAL | · | · | ⊙ | ok |
| `GET /Users/Me` | registered | REAL | ✅ | ✅ | ✅ | ok |
| `POST /Users/New` | registered | REAL | · | · | ⊙ | ok |
| `POST /Users/Password` | registered | REAL | · | · | ⊙ | ok |
| `GET /Users/Public` | registered | REAL | ✅ | ✅ | · |  |
| `DELETE /Users/{userId}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Users/{userId}` | registered | REAL | ✅ | ✅ | ⊙ | ok |
| `POST /Users/{userId}/Policy` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Videos/ActiveEncodings` | registered | REAL | · | · | ⊙ | ok |
| `POST /Videos/MergeVersions` | registered | REAL | · | · | ⊙ | ok |
| `GET /Videos/{itemId}/AdditionalParts` | registered | REAL | ✅ | ✅ | ∅ |  |
| `DELETE /Videos/{itemId}/AlternateSources` | registered | REAL | · | · | ⊙ | ok |
| `POST /Videos/{itemId}/Subtitles` | registered | REAL | · | · | ⊙ | ok |
| `DELETE /Videos/{itemId}/Subtitles/{index}` | registered | REAL | · | · | ⊙ | ok |
| `GET /Videos/{itemId}/Trickplay/{width}/tiles.m3u8` | registered | REAL | ✅ | · | ⚠️ | flagged: stream signature diff vs Jellyfin (verify) |
| `GET /Videos/{itemId}/Trickplay/{width}/{index}.jpg` | registered | REAL | ✅ | · | ⚠️ | flagged: stream signature diff vs Jellyfin (verify) |
| `GET /Videos/{itemId}/hls/{playlistId}/stream.m3u8` | registered | REAL | · | · | ▫ |  |
| `GET /Videos/{itemId}/hls/{playlistId}/{segmentId}.{segmentContainer}` | registered | REAL | · | · | ▫ |  |
| `GET /Videos/{itemId}/hls1/{playlistId}/{segmentId}.{container}` | registered | REAL | · | · | ◐ |  |
| `GET /Videos/{itemId}/live.m3u8` | registered | REAL | ⚠️ | · | ◐ |  |
| `GET /Videos/{itemId}/main.m3u8` | registered | REAL | ✅ | · | ◐ |  |
| `GET /Videos/{itemId}/master.m3u8` | registered | REAL | ✅ | · | ◐ |  |
| `HEAD /Videos/{itemId}/master.m3u8` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Videos/{itemId}/stream` | registered | REAL | ✅ | · | ✅ |  |
| `HEAD /Videos/{itemId}/stream` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Videos/{itemId}/stream.{container}` | registered | REAL | ✅ | · | ✅ |  |
| `HEAD /Videos/{itemId}/stream.{container}` | registered | REAL | ✅ | · | ▫ |  |
| `GET /Videos/{itemId}/{mediaSourceId}/Subtitles/{index}/subtitles.m3u8` | registered | REAL | ✅ | · | ◐ |  |
| `GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/Stream.{routeFormat}` | registered | REAL | ⚠️ | · | ✅ |  |
| `GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/{routeStartPositionTicks}/Stream.{routeFormat}` | registered | REAL | ⚠️ | · | ✅ |  |
| `GET /Videos/{videoId}/{mediaSourceId}/Attachments/{index}` | registered | REAL | ✅ | · | ✅ |  |
| `GET /Years` | registered | REAL | ✅ | ✅ | ⚠️ | expected-extension: Ferrofin does not materialize Year items (documented out-of-scope). |
| `GET /Years/{year}` | registered | REAL | ✅ | ✅ | ⚠️ | expected-extension: Ferrofin does not materialize Year items (documented out-of-scope; matches the GET /Years classification), so a single year has no item to return (H=404 vs J=200). |
| `GET /web/ConfigurationPage` | registered | REAL | ✅ | · | · | expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises. |
| `GET /web/ConfigurationPages` | registered | REAL | ✅ | ✅ | ⚠️ | expected-extension: plugin configuration pages come from external plugins Ferrofin doesn't host. |
