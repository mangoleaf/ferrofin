{
 "_comment": "Curated ACCEPTED divergences (not bugs). Highest-precedence classification in gen-ledger: overrides the auto 'flagged: verify' from the Layer-2 read diff. deep_verified stays as the live layers report it (these do not diff clean vs Jellyfin by design). Sourced from the parity-triage workflow verdicts (parity/triage-findings.json) + the roadmap synthesis, minus the ops since fixed (Filters2 WI-2, Collections add/remove WI-6, Images Size WI-1).",
 "last_verified": "2026-08-31",
 "rows": {
  "DELETE /Intros/Show/{SeriesId}/{SeasonId}": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "DELETE /LiveTv/ListingProviders": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture's listings provider and observes the guide emptying."
  },
  "DELETE /LiveTv/SeriesTimers/{timerId}": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete)."
  },
  "DELETE /LiveTv/TunerHosts": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a journey that removes the fixture tuner and observes its channels going."
  },
  "DELETE /MediaSegmentsApi/{segmentId}": {
   "category": "expected-extension",
   "classification": "expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming."
  },
  "DELETE /Packages/Installing/{packageId}": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: always 404 — installs are synchronous, so no tracked installation exists to cancel (Jellyfin tracks async installs). Admin-only."
  },
  "DELETE /Playlists/{playlistId}/Users/{userId}": {
   "category": "instance",
   "classification": "instance/methodology: Ferrofin correctly removes the playlist share (H read-back shows it gone); Jellyfin returns 204 but still lists the user (Jellyfin quirk), so Ferrofin is the more-correct side — not a defect."
  },
  "DELETE /Plugins/{pluginId}": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: real uninstall for a staged WASM plugin (file + config + enabled override removed, restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only."
  },
  "DELETE /Plugins/{pluginId}/{version}": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: real uninstall for a staged WASM plugin (restart-required); a compiled-in extension honestly rejects with 400 where Jellyfin can uninstall any plugin. Admin-only."
  },
  "GET /Albums/{itemId}/Similar": {
   "category": "instance",
   "classification": "flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. GREEN BUT VACUOUS ON THE CURRENT FIXTURE — read this row as 'no divergence detected', not as 'verified'. `suite/perf/gen-fixtures.sh` gave each of the 3 albums a UNIQUE music genre, so the candidate universe is empty by construction on BOTH servers and the leg diffs `{\"Items\": [], \"TotalRecordCount\": 0}` against itself. The fixture is changed (two of the three artists now share a genre) but that only takes effect at the campaign's final wipe-and-provision; until that sweep runs, the album matching itself is covered by the `an_album_seed_matches_albums_sharing_a_genre_only` unit test (two albums sharing a genre, a same-genre track that must NOT come back because IncludeItemTypes is [MusicAlbum] alone, and the ExcludeArtistIds leg with a positive control). A real Ferrofin bug found behind this row IS fixed and verified live: album Genres/Studios/PremiereDate are inherited from the album's Audio children (C# AlbumMetadataService + MetadataService.UpdateGenres/UpdateStudios/UpdatePremiereDate); Ferrofin aggregated only album-artist and year, so every album had `Genres: []` and was unreachable by the Genres= query this endpoint issues. After the fix, F album Genres/PremiereDate/ProductionYear match J exactly (Jazz/Rock/Ambient, 2022-01-01T00:00:00.0000000Z, 2022)."
  },
  "GET /Artists": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (RunTimeTicks / CumulativeRunTimeTicks ONLY; every other leg of this row now diffs clean by measurement). `MusicArtistResolver` is PORTED (library_scan.rs::plan_music_node + is_music_artist, a port of v10.11.8 Emby.Server.Implementations/Library/Resolvers/Audio/MusicArtistResolver.cs:54-125), so an artist directory now resolves to a real MusicArtist row parented into the music library, on Jellyfin's OWN path-derived id (measured byte-identical on the fixture: Artist 01 = 0f39932400340734bea41ffc13aaddc4, Artist 02 = 2b3868bfaa266cc1d3cc4c3dd5d40800, Artist 03 = 697d2b7b6ec56d3cf7e87c2de748b0cd), with its albums re-parented onto it. That closed Path, CanDelete, RecursiveItemCount and the empty user-scoped MusicArtist universe.\n\nWHAT REMAINS, and why it is Jellyfin's bug and not Ferrofin's: with the artist resolved as a folder, `MetadataService.UpdateCumulativeRunTimeTicks` (MediaBrowser.Providers/Manager/MetadataService.cs:451) sums the RunTimeTicks of the non-folder children returned by `GetChildrenForMetadataUpdates`. For a MusicArtist that method is OVERRIDDEN — `ArtistMetadataService.GetChildrenForMetadataUpdates` (MediaBrowser.Providers/Music/ArtistMetadataService.cs:44-56) returns `item.IsAccessedByName ? item.GetTaggedItems(Recursive, IsFolder=false) : item.GetRecursiveChildren(i => i is IHasArtist && !i.IsFolder)`, NOT the base class's plain `folder.GetRecursiveChildren()` at MetadataService.cs:397-401. A folder-resolved artist takes the second arm, and `Audio : IHasArtist`, so the set is exactly the three tracks — the override strengthens the conclusion rather than changing it, and it is cited here because re-deriving the number from the base method lands on the wrong call. `MusicArtist.SupportsCumulativeRunTimeTicks => true` (MusicArtist.cs:38). 3 tracks x 20000000 = 60000000, which is what Ferrofin stores. The 10.11.8 oracle stores 0, and its OWN DateLastRefreshed timestamps show why — a first-scan ordering race, read out of this lab's /config/data/jellyfin.db:\n  MusicArtist 'Artist 01'   RunTimeTicks 0         DateLastRefreshed 2026-08-30 00:29:30.8359533\n  Audio       'Track 01'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8815808\n  Audio       'Track 02'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8973703\n  Audio       'Track 03'    RunTimeTicks 20000000                    2026-08-30 00:29:30.9096119\n  MusicAlbum  'Album 01'    RunTimeTicks 60000000                    2026-08-30 00:29:30.9187306\nThe artist refreshed 46 ms BEFORE its first track was probed, so it summed a set of NULLs. The album — identical code, `SupportsCumulativeRunTimeTicks` also true, refreshed after all three — got the right number. A rescan does not repair it either: `UpdateMetadataFromChildren` is gated on `isFullRefresh || currentUpdateType > None`. Matching Jellyfin here would mean reproducing a first-scan race on purpose.\n\nKept per 'don't port Jellyfin's bugs' (docs/ + the standing memory). OWNER CALL PENDING: ratify keeping 60000000, or direct that Ferrofin reproduce the race. The ledger renders this row under 'Awaiting owner ratification', NOT under 'accepted' — that bucket is a claim about a decision somebody made, and this one has not been made. Until ratified the row stays deep_verified=false against this oracle, honestly — the divergence is REAL and visible, not hidden. parity_diff.VOLATILE was NOT widened for RunTimeTicks and must not be.\n\nROW HISTORY, kept rather than dropped: before the port this row's bodies DIFFED CLEAN, and the old classification's warning ('DO NOT BANK THIS ROW') was right — the agreement was two different mechanisms landing on the same number. Ferrofin wrote RunTimeTicks 0 because its artist was a parentless by-name row with no recursive children and the faithful UpdateCumulativeRunTimeTicks summed nothing; Jellyfin wrote 0 because of the race above. The green was a coincidence and it is now a visible mismatch for the right reason. DE-DUPLICATION CHECK for this row specifically: it must return exactly 3 artists. A previous attempt at this port was rolled back because `push_by_name_join` joins on CleanName and the scanned row plus the by-name row each produced an entry, listing every artist twice. Measured 3, twice, after the fix."
  },
  "GET /Artists/AlbumArtists": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (RunTimeTicks / CumulativeRunTimeTicks ONLY; every other leg of this row now diffs clean by measurement). `MusicArtistResolver` is PORTED (library_scan.rs::plan_music_node + is_music_artist, a port of v10.11.8 Emby.Server.Implementations/Library/Resolvers/Audio/MusicArtistResolver.cs:54-125), so an artist directory now resolves to a real MusicArtist row parented into the music library, on Jellyfin's OWN path-derived id (measured byte-identical on the fixture: Artist 01 = 0f39932400340734bea41ffc13aaddc4, Artist 02 = 2b3868bfaa266cc1d3cc4c3dd5d40800, Artist 03 = 697d2b7b6ec56d3cf7e87c2de748b0cd), with its albums re-parented onto it. That closed Path, CanDelete, RecursiveItemCount and the empty user-scoped MusicArtist universe.\n\nWHAT REMAINS, and why it is Jellyfin's bug and not Ferrofin's: with the artist resolved as a folder, `MetadataService.UpdateCumulativeRunTimeTicks` (MediaBrowser.Providers/Manager/MetadataService.cs:451) sums the RunTimeTicks of the non-folder children returned by `GetChildrenForMetadataUpdates`. For a MusicArtist that method is OVERRIDDEN — `ArtistMetadataService.GetChildrenForMetadataUpdates` (MediaBrowser.Providers/Music/ArtistMetadataService.cs:44-56) returns `item.IsAccessedByName ? item.GetTaggedItems(Recursive, IsFolder=false) : item.GetRecursiveChildren(i => i is IHasArtist && !i.IsFolder)`, NOT the base class's plain `folder.GetRecursiveChildren()` at MetadataService.cs:397-401. A folder-resolved artist takes the second arm, and `Audio : IHasArtist`, so the set is exactly the three tracks — the override strengthens the conclusion rather than changing it, and it is cited here because re-deriving the number from the base method lands on the wrong call. `MusicArtist.SupportsCumulativeRunTimeTicks => true` (MusicArtist.cs:38). 3 tracks x 20000000 = 60000000, which is what Ferrofin stores. The 10.11.8 oracle stores 0, and its OWN DateLastRefreshed timestamps show why — a first-scan ordering race, read out of this lab's /config/data/jellyfin.db:\n  MusicArtist 'Artist 01'   RunTimeTicks 0         DateLastRefreshed 2026-08-30 00:29:30.8359533\n  Audio       'Track 01'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8815808\n  Audio       'Track 02'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8973703\n  Audio       'Track 03'    RunTimeTicks 20000000                    2026-08-30 00:29:30.9096119\n  MusicAlbum  'Album 01'    RunTimeTicks 60000000                    2026-08-30 00:29:30.9187306\nThe artist refreshed 46 ms BEFORE its first track was probed, so it summed a set of NULLs. The album — identical code, `SupportsCumulativeRunTimeTicks` also true, refreshed after all three — got the right number. A rescan does not repair it either: `UpdateMetadataFromChildren` is gated on `isFullRefresh || currentUpdateType > None`. Matching Jellyfin here would mean reproducing a first-scan race on purpose.\n\nKept per 'don't port Jellyfin's bugs' (docs/ + the standing memory). OWNER CALL PENDING: ratify keeping 60000000, or direct that Ferrofin reproduce the race. The ledger renders this row under 'Awaiting owner ratification', NOT under 'accepted' — that bucket is a claim about a decision somebody made, and this one has not been made. Until ratified the row stays deep_verified=false against this oracle, honestly — the divergence is REAL and visible, not hidden. parity_diff.VOLATILE was NOT widened for RunTimeTicks and must not be.\n\nROW HISTORY, kept rather than dropped: this row also diffed clean before the port, for the same coincidence recorded on `GET /Artists` (two mechanisms landing on 0) — the old classification said not to bank it and that was correct. It is now clean on every leaf but the ratified one, for the right reason."
  },
  "GET /Artists/{itemId}/Similar": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (RunTimeTicks / CumulativeRunTimeTicks ONLY; every other leg of this row now diffs clean by measurement). `MusicArtistResolver` is PORTED (library_scan.rs::plan_music_node + is_music_artist, a port of v10.11.8 Emby.Server.Implementations/Library/Resolvers/Audio/MusicArtistResolver.cs:54-125), so an artist directory now resolves to a real MusicArtist row parented into the music library, on Jellyfin's OWN path-derived id (measured byte-identical on the fixture: Artist 01 = 0f39932400340734bea41ffc13aaddc4, Artist 02 = 2b3868bfaa266cc1d3cc4c3dd5d40800, Artist 03 = 697d2b7b6ec56d3cf7e87c2de748b0cd), with its albums re-parented onto it. That closed Path, CanDelete, RecursiveItemCount and the empty user-scoped MusicArtist universe.\n\nWHAT REMAINS, and why it is Jellyfin's bug and not Ferrofin's: with the artist resolved as a folder, `MetadataService.UpdateCumulativeRunTimeTicks` (MediaBrowser.Providers/Manager/MetadataService.cs:451) sums the RunTimeTicks of the non-folder children returned by `GetChildrenForMetadataUpdates`. For a MusicArtist that method is OVERRIDDEN — `ArtistMetadataService.GetChildrenForMetadataUpdates` (MediaBrowser.Providers/Music/ArtistMetadataService.cs:44-56) returns `item.IsAccessedByName ? item.GetTaggedItems(Recursive, IsFolder=false) : item.GetRecursiveChildren(i => i is IHasArtist && !i.IsFolder)`, NOT the base class's plain `folder.GetRecursiveChildren()` at MetadataService.cs:397-401. A folder-resolved artist takes the second arm, and `Audio : IHasArtist`, so the set is exactly the three tracks — the override strengthens the conclusion rather than changing it, and it is cited here because re-deriving the number from the base method lands on the wrong call. `MusicArtist.SupportsCumulativeRunTimeTicks => true` (MusicArtist.cs:38). 3 tracks x 20000000 = 60000000, which is what Ferrofin stores. The 10.11.8 oracle stores 0, and its OWN DateLastRefreshed timestamps show why — a first-scan ordering race, read out of this lab's /config/data/jellyfin.db:\n  MusicArtist 'Artist 01'   RunTimeTicks 0         DateLastRefreshed 2026-08-30 00:29:30.8359533\n  Audio       'Track 01'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8815808\n  Audio       'Track 02'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8973703\n  Audio       'Track 03'    RunTimeTicks 20000000                    2026-08-30 00:29:30.9096119\n  MusicAlbum  'Album 01'    RunTimeTicks 60000000                    2026-08-30 00:29:30.9187306\nThe artist refreshed 46 ms BEFORE its first track was probed, so it summed a set of NULLs. The album — identical code, `SupportsCumulativeRunTimeTicks` also true, refreshed after all three — got the right number. A rescan does not repair it either: `UpdateMetadataFromChildren` is gated on `isFullRefresh || currentUpdateType > None`. Matching Jellyfin here would mean reproducing a first-scan race on purpose.\n\nKept per 'don't port Jellyfin's bugs' (docs/ + the standing memory). OWNER CALL PENDING: ratify keeping 60000000, or direct that Ferrofin reproduce the race. The ledger renders this row under 'Awaiting owner ratification', NOT under 'accepted' — that bucket is a claim about a decision somebody made, and this one has not been made. Until ratified the row stays deep_verified=false against this oracle, honestly — the divergence is REAL and visible, not hidden. parity_diff.VOLATILE was NOT widened for RunTimeTicks and must not be.\n\nWHAT THIS ROW CLOSED: Ferrofin returned 0 rows where Jellyfin returned 2. The scorer half was already right (`MusicArtistSimilarItemsProvider` is a flat InternalItemsQuery { Genres, Tags, IncludeItemTypes=[MusicArtist] } and an EMPTY Genres/Tags pair is not a predicate, so an untagged artist matches every other artist — covered by the `an_untagged_artist_seed_still_matches_every_other_artist` unit test). The blocker was the item UNIVERSE: no MusicArtist row survived `scope_to_user_libraries` (a faithful `AddUserToQuery`, TopParentId IS NOT NULL). That filter was NOT relaxed; the scanner was fixed. Both peers now return on both servers.\n\nORDERING — this row's own methodology guard rail, and it is LIVE now that the candidate pool is no longer empty: this endpoint's provider orders `Random` on BOTH servers (MusicArtistSimilarItemsProvider.cs:50 / v10.11.8 LibraryController.cs:801) and Ferrofin emits `Random` too. parity_diff aligns arrays by key, so the ORDER is not gateable by any probe — which means the reads leg MUST stay pinned at a limit above the candidate pool, or the two servers will draw different SUBSETS and the row will diff on membership for a reason that is not a bug. reads.py:636 pins limit=1000 (rationale at reads.py:608-623). Do not lower it, and do not 'fix' a future red on this row by narrowing the compare."
  },
  "GET /Artists/{name}": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (RunTimeTicks / CumulativeRunTimeTicks ONLY; every other leg of this row now diffs clean by measurement). `MusicArtistResolver` is PORTED (library_scan.rs::plan_music_node + is_music_artist, a port of v10.11.8 Emby.Server.Implementations/Library/Resolvers/Audio/MusicArtistResolver.cs:54-125), so an artist directory now resolves to a real MusicArtist row parented into the music library, on Jellyfin's OWN path-derived id (measured byte-identical on the fixture: Artist 01 = 0f39932400340734bea41ffc13aaddc4, Artist 02 = 2b3868bfaa266cc1d3cc4c3dd5d40800, Artist 03 = 697d2b7b6ec56d3cf7e87c2de748b0cd), with its albums re-parented onto it. That closed Path, CanDelete, RecursiveItemCount and the empty user-scoped MusicArtist universe.\n\nWHAT REMAINS, and why it is Jellyfin's bug and not Ferrofin's: with the artist resolved as a folder, `MetadataService.UpdateCumulativeRunTimeTicks` (MediaBrowser.Providers/Manager/MetadataService.cs:451) sums the RunTimeTicks of the non-folder children returned by `GetChildrenForMetadataUpdates`. For a MusicArtist that method is OVERRIDDEN — `ArtistMetadataService.GetChildrenForMetadataUpdates` (MediaBrowser.Providers/Music/ArtistMetadataService.cs:44-56) returns `item.IsAccessedByName ? item.GetTaggedItems(Recursive, IsFolder=false) : item.GetRecursiveChildren(i => i is IHasArtist && !i.IsFolder)`, NOT the base class's plain `folder.GetRecursiveChildren()` at MetadataService.cs:397-401. A folder-resolved artist takes the second arm, and `Audio : IHasArtist`, so the set is exactly the three tracks — the override strengthens the conclusion rather than changing it, and it is cited here because re-deriving the number from the base method lands on the wrong call. `MusicArtist.SupportsCumulativeRunTimeTicks => true` (MusicArtist.cs:38). 3 tracks x 20000000 = 60000000, which is what Ferrofin stores. The 10.11.8 oracle stores 0, and its OWN DateLastRefreshed timestamps show why — a first-scan ordering race, read out of this lab's /config/data/jellyfin.db:\n  MusicArtist 'Artist 01'   RunTimeTicks 0         DateLastRefreshed 2026-08-30 00:29:30.8359533\n  Audio       'Track 01'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8815808\n  Audio       'Track 02'    RunTimeTicks 20000000                    2026-08-30 00:29:30.8973703\n  Audio       'Track 03'    RunTimeTicks 20000000                    2026-08-30 00:29:30.9096119\n  MusicAlbum  'Album 01'    RunTimeTicks 60000000                    2026-08-30 00:29:30.9187306\nThe artist refreshed 46 ms BEFORE its first track was probed, so it summed a set of NULLs. The album — identical code, `SupportsCumulativeRunTimeTicks` also true, refreshed after all three — got the right number. A rescan does not repair it either: `UpdateMetadataFromChildren` is gated on `isFullRefresh || currentUpdateType > None`. Matching Jellyfin here would mean reproducing a first-scan race on purpose.\n\nKept per 'don't port Jellyfin's bugs' (docs/ + the standing memory). OWNER CALL PENDING: ratify keeping 60000000, or direct that Ferrofin reproduce the race. The ledger renders this row under 'Awaiting owner ratification', NOT under 'accepted' — that bucket is a claim about a decision somebody made, and this one has not been made. Until ratified the row stays deep_verified=false against this oracle, honestly — the divergence is REAL and visible, not hidden. parity_diff.VOLATILE was NOT widened for RunTimeTicks and must not be.\n\nWHAT THIS ROW CLOSED, with the before-values: Path (J '/media/synth/music/Artist 01' vs H '/config/metadata/artists/Artist 01' — H's old value was the CORRECT path for the by-name row it actually had), CanDelete (J true, H false — kinds::can_delete is already a faithful !IsAccessedByName), and a missing RecursiveItemCount (J 3). All three measured equal now.\n\nTWO DIVERGENCES THIS ROW STILL CARRIES that the harness cannot see, recorded here so they are not mistaken for settled. Both are masked by parity_diff.VOLATILE, which was NOT widened for either — they were already in it. (1) ParentId: Jellyfin materializes a physical `Folder` row per media path and parents the artist to it (F 0 such rows vs J 9); Ferrofin parents to the CollectionFolder. The id derivation already reproduces exactly — md5_le('MediaBrowser.Controller.Entities.Folder' + '/media/synth/music') = f87561f1dd83cf1f019883e008f5f307, which is the id Jellyfin stores — so closing it is mechanical, but it changes the ancestor chain of every scanned item and needs its own measured change plus the perf gate. (2) UserData.Key: C# UserDataManager.cs:239-245 uses item.GetUserDataKeys()[0], so Jellyfin emits 'Artist-Artist 01' where Ferrofin emits the raw guid '0f399324-0034-0734-bea4-1ffc13aaddc4'. kinds.rs and user_data_keys.rs already COMPUTE the right keys; the gap is plumbing in user_data_manager.rs::empty_row / dto_service.rs::empty_user_data_dto, and it is server-wide, not artist-specific."
  },
  "GET /Audio/{itemId}/RemoteSearch/Lyrics": {
   "category": "expected-extension",
   "classification": "expected-extension: Jellyfin 10.11.8 ships NO ILyricProvider implementation — `git grep \": ILyricProvider\" v10.11.8 -- '*.cs'` returns nothing (only the interface, LyricManager and the CoreAppHost DI scan), and that server's own GET /Libraries/AvailableOptions reports `LyricFetchers: []`. Ferrofin compiles LrcLib in the way it compiles TMDB in. On this fixture both answer `200 []` — Jellyfin structurally, Ferrofin because the synthetic titles (\"Artist 01 Album 01 Track 01\") match no lrclib record (egress verified: `curl https://lrclib.net/api/search` returns 200 from BOTH containers, so this is a corpus fact, not a network gap). The row therefore agrees on an empty list and compares nothing; only a real-track fixture would give it content, and then only Ferrofin could return any."
  },
  "GET /Channels/{channelId}/Features": {
   "category": "requires-channel-plugin",
   "classification": "requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers."
  },
  "GET /Channels/{channelId}/Items": {
   "category": "requires-channel-plugin",
   "classification": "requires-channel-plugin: the ChannelsController serves IChannel plugin channels, not Live TV — no tuner journey can reach these; they need a channel plugin on both servers."
  },
  "GET /Environment/Drives": {
   "category": "instance",
   "classification": "instance: per-host filesystem drives."
  },
  "GET /Episode/{Id}/Timestamps": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /Episode/{id}/IntroSkipperSegments": {
   "category": "expected-extension",
   "classification": "expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming."
  },
  "GET /Genres": {
   "category": "open-work",
   "classification": "open-work (NOT accepted) — SUPERSEDES the 2026-08-11 'verified: QueryResult<BaseItemDto>, status+schema conformant' seed (a shape check that never diffed the listing) and the 2026-08-31 revision of it. Measured twice on 2026-08-31 after the by-name ports: H lists [Action, Ambient, Comedy, Drama, Jazz, Rock, SciFi, Thriller], J lists [Action, Comedy, Drama, Jazz, News, SciFi, Thriller]. Two separate axes, one upstream and one a REAL Ferrofin gap that is named here rather than hidden.\n\nAXIS 1 (H's 'extra' Ambient and Rock) — the same mechanism as GET /MusicGenres axis 1, and NOT a missing port: upstream's `GroupBy(PresentationUniqueKey)` collapse IS now implemented in Ferrofin (`item_repository::push_representative_rank`, see that row). Both servers hold Genre rows named Ambient/Jazz/Rock; on Jellyfin all three were lazily created by `CreateItemByName` at 2026-08-31 01:16:57 with a NULL PresentationUniqueKey and a NULL DateLastRefreshed (read out of the container's own DB), so they form one NULL group and surface as the single representative Jazz. Ferrofin's carry Genre-Ambient/-Jazz/-Rock — the value `MetadataService.BeforeSaveInternal` would give them on their first refresh — so all three list. On a freshly provisioned pair neither server holds those rows at all, so this axis should not appear in the end-of-campaign sweep.\n\nAXIS 2 (FERROFIN GAP, open work, NOT accepted) — J lists a genre 'News' that H does not. Root cause measured, not guessed: J's `news` value is carried by 676 `LiveTvProgram` rows plus 15 Movie rows (its EPG), while Ferrofin has NO ItemValues row for `news` at all — because Ferrofin stores EPG programmes in its own `FerrofinLiveTvPrograms` table (ferrofin-livetv/src/manager.rs:542) and never materializes them as `BaseItems`, so they contribute to no by-name aggregate. Upstream persists a programme through `BaseItemRepository.SaveItems`, whose second half writes `ItemValues`/`ItemValuesMap` for it, which is why every EPG genre shows on Jellyfin's /Genres. UN-DEFER PATH: make the guide refresh write a `LiveTvProgram` BaseItems row per programme (or, narrower, project the guide's genres into `ItemValues` alongside the FerrofinLiveTvPrograms row) and re-measure this row plus /Items/Filters2. Still NOT done and not claimed as done — it is a Live TV storage-model change, outside the four by-name rows batch F2 owns."
  },
  "GET /Genres/{genreName}": {
   "category": "verified",
   "classification": "verified 2026-08-31 (batch F2, revised after review): 4/4 legs clean, 108 fields compared, measured twice. SUPERSEDES both the batch-B1 record (true about the lazy create, blind to everything else — it probed ONE name) and the 2026-08-31 'accepted-divergence' record, whose accepted divergence has been REMOVED from the code rather than argued for.\n\n`GenresController.GetGenre` has THREE behaviours on both trees (v10.11.8 :159-208; master's only file delta is dropping `.AddClientFields(User)` and adding `[Tags(\"Genre\")]`):\n  (a) a plain name -> `_libraryManager.GetGenre(name)` = `CreateItemByName<Genre>` (ported by B1);\n  (b) a name containing `BaseItem.SlugChar` ('-', BaseItem.cs:98 on v10.11.8, :100 on master) -> `GetItemFromSlugName<T>`: EXACTLY three plain `GetItemList` lookups substituting '-'->'&', then '/', then '?', which create NOTHING;\n  (c) no match -> `item ??= new Genre()`, a default-constructed row — the route cannot 404.\nFerrofin had only (a), so a hyphenated name MINTED a bogus genre: measured before the fix, `GET /Genres/Diag-Slug-F2x3` returned H {Name, Path /config/metadata/Genre/Diag-Slug-F2x3, SortName, LocationType FileSystem} against J's all-zero DTO. Ported as `by_name::resolve_by_name_or_slug` + the non-creating `LibraryManager::find_named_item` (core: `resolve_named_rows`, the resolve half of `get_named_items` with the CreateItemByName write left off) + `LibraryManager::empty_by_name_item`. Verified live that it creates nothing: after five reads.py runs across two builds, the only `Diag`-named genre in Ferrofin's BaseItems is the `Diag-Slug-F2x3` row minted by the OLD build, and no `Diag-No-Such-F2` row exists on either side.\n\nFixing (c) exposed a THIRD, wider defect this row is the only one that could see: `DtoService` derived `LocationType` from `IsVirtualItem`, where C# `BaseItem.LocationType` (v10.11.8 BaseItem.cs:319-336, master :327-344, identical) derives it from the PATH — empty path -> Virtual, a path with a scheme other than `file://` -> Remote (`ManagedFileSystem.IsPathFile`, :526-535), otherwise FileSystem. Ferrofin therefore said FileSystem for every pathless row. Ported as `dto_service::location_type_of`; measured on the empty item J=Virtual H=Virtual.\n\nRECORDED UPSTREAM QUIRK (Ferrofin now reproduces it, deliberately). Because branch (b) reads a hyphen as a slug and nothing else, a genre whose real name CONTAINS a hyphen is unreachable through this route on both trees: `/Genres/Sci-Fi` answers with the empty DTO of (c) even when a genre named exactly `Sci-Fi` exists. The first cut of this port added a fourth, non-creating lookup on the literal name to reach it, and recorded that as an accepted divergence justified by 'don't port Jellyfin's bugs' plus the campaign rule against deleting a capability Ferrofin ships. Both justifications were wrong and the fourth lookup has been DROPPED: `GetItemFromSlugName` is byte-identical on v10.11.8 and master and stops after '?', so a fourth candidate matched NEITHER tree — the campaign's own definition of the bug — and the pre-existing behaviour the 'capability' argument pointed at was the create-on-GET defect this batch removed, not a shipped feature. No probe leg could observe the difference either (the fixture has no hyphenated genre), which is exactly why it had to be settled by the C# rather than by the wire. Pinned by the unit test `by_name_browse::a_hyphenated_name_is_only_ever_read_as_a_slug`, whose fake resolves a genre literally named `Sci-Fi` so a returning fourth candidate fails it.\n\nFIXTURE: `suite/perf/gen-fixtures.sh` seeds a genre literally named `R&B` so the '&' substitution is probed on the wire (reads.py legs `/Genres/R-B` and `/Genres/Diag-No-Such-F2`). It is ADDED to every 50th movie rather than folded into the genre rotation: widening the rotation from 5 to 6 repartitions the whole shared corpus (500/5 -> 500/6 movies per genre) at the next wipe-and-provision, which would stale every count other batches recorded — including this campaign's own '200 movies carry action' — and make suite/perf-baseline.json non-comparable for the mandatory end-of-campaign perf gate. Verified by simulation that all five original genres keep exactly 200 movies and `R&B` gains 10. Until the fixture is re-provisioned both slug legs exercise the (c) empty-item path, which is a real agreement but not the substitution."
  },
  "GET /IntroSkipper": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /IntroSkipper/SupportBundle": {
   "category": "expected-extension",
   "classification": "expected-extension: IntroSkipper is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (H=200 vs J=404). By design."
  },
  "GET /Intros/AnalyzerActions/{SeasonId}": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /Intros/ScanStatus": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /Intros/Show/{SeriesId}/{SeasonId}": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /Items/Suggestions": {
   "category": "open-work",
   "classification": "open-work (NOT accepted) — but the MusicArtist half of this row is CLOSED. `MusicArtistResolver` is now ported, so `/Items/Suggestions?type=MusicArtist` returns the same 3 artists on both servers (measured twice: F 3 ['Artist 01','Artist 02','Artist 03'], J the same) where Ferrofin used to return 0, and the three `Items[Path=/media/synth/music/Artist NN] (whole item)` misses are gone. The artist rows now carry the RunTimeTicks divergence classified on `GET /Artists` (a Jellyfin first-scan race; 60000000 is correct). What is still open on this row belongs to OTHER causes and OTHER rows: the universe kinds Ferrofin does not materialize at all (physical Folder 0/9, TvChannel, UserView, LiveTvProgram), `SeriesStudio` on Episode, the MediaStreams/MediaSources bitrate offsets already classified on `GET /Items/{itemId}`, and the movie/playlist corpus asymmetry between the two long-lived instances. `RecursiveItemCount` and folder `UserData.PlayedPercentage` ARE now ported (dto_service.rs, a port of Folder.cs:1798-1836). Do NOT widen VOLATILE or drop a leg to hide any of the rest.\n\n--- previous text, retained ---\nopen-work (NOT accepted). The old text ('Random, so the set is non-deterministic') was only half true and it hid two real divergences. Random here PERMUTES but does not change the set: `SuggestionsController` runs ONE query (`OrderBy = [(Random, Descending)]`, Recursive, mediaType/type, IsVirtualItem=false) and Ferrofin's handler is a line-for-line port, so at `limit >= |universe|` the answer is fully determined and parity_diff aligns Items by Path. The probe was the problem: `limit=10` sampled 10 of 552 at random and could never diff. It is now eight legs: seven pinned at a full limit (per type), where Random only permutes and the whole body IS diffed, plus one narrow startIndex/limit leg. That narrow leg is the one place a projection is applied — a page of 3 out of 500 Random-ordered rows draws a different triple per call on BOTH servers (measured: three consecutive calls, three different triples each side), so it compares StartIndex, TotalRecordCount and the page length only. Diffing its Items would pin this row red forever and destroy its ability to signal when the two real divergences below are fixed; the projection is scoped to that leg alone and nothing was added to parity_diff.VOLATILE. Two causes remain, BOTH owned by other rows and deliberately left visible here rather than filtered out of the probe: (1) ITEM UNIVERSE — Ferrofin's recursive user-root universe has no MusicArtist, Folder, TvChannel or UserView rows (F 552 vs J 561 unfiltered; per type F 0 vs J 3/3/2/1), the same parentless-by-name/TopParentId cause as GET /Artists/{itemId}/Similar; detector `/Items?userId=<u>&recursive=true&limit=100000`, which shows the identical 9-row delta, so it is not this handler's bug. (2) PER-ITEM DTO FIELDS — MediaSources[].Name/Bitrate, MediaStreams[].BitRate (a constant 192000 offset), LocalizedLanguage, Level/IsAVC/Width/Height: the same set already classified on GET /Items/{itemId} as independent-probe noise, inherited here because Suggestions returns full item DTOs. Narrowing the compare to hide either would be the dishonest green; the row stays red until they are fixed on their own rows. The multi-leg probe no longer skips a leg whose servers disagree on status: a status divergence (or a body that is not 200-with-JSON on both sides) is recorded as a mismatch, which is the whole point of keeping the type=MusicArtist and type=TvChannel legs. UNIVERSE GAP, re-measured 2026-08-29 and WIDER than first written: LiveTvProgram belongs on the missing-kind list too — `/Items?userId=<u>&recursive=true&includeItemTypes=LiveTvProgram` returns 338 rows on Jellyfin and 0 on Ferrofin, while `/LiveTv/Programs` returns the guide on both (F 19200, J 338). The knock-on is visible on a THIRD row: GET /Genres is red only because Jellyfin's genre list carries `News`, the XMLTV guide's programme genre, which Ferrofin's genre list cannot see for the same reason. One fix (give the by-name/programme rows a TopParentId so `scope_to_user_libraries` stops filtering them out) closes /Artists Similar, these Suggestions legs and the /Genres row together; it changes the recursive item universe server-wide, so it is its own change WITH the perf gate."
  },
  "GET /Items/{itemId}": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. NOTE: the previous 'instance/probe-noise' text on this row was DISPROVEN in batch A3 — running both containers' ffprobe binaries over the same file gives byte-identical JSON (7.1.4 vs 7.1.3-Jellyfin), and ffprobe emits no per-stream bit_rate at all. The five other residuals it covered were real server bugs and are fixed: MediaSources[].Name now ports BaseItem.GetMediaSourceName (file stem, not item.Name); codec_tag_string binds the correct 10.11.8 JSON key (the trailing-'?' typo is master-only); Level/IsAVC are set on every stream and subtitle Width/Height default to 0, matching C#'s non-nullable probing DTO; LocalizedLanguage is no longer serialized (off-contract, the cause of schema_valid:false); and the People[].Role read projection no longer NULLIFs the empty string C# always emits."
  },
  "GET /Items/{itemId}/Ancestors": {
   "category": "open-work",
   "classification": "open-work (NOT accepted): with both containers on the same data dir the ancestor rows now align by Path and every field matches except two, BOTH on the UserRootFolder hop. (1) ChildCount J=4 H=3 — UserRootFolder.GetEligibleChildrenForRecursiveChildren (UserRootFolder.cs:96-102) concatenates LibraryManager.RootFolder.VirtualChildren, which is the PlaylistsFolder CreateRootFolder adds at {data}/playlists (LibraryManager.cs:1090-1120); Ferrofin stores that folder parentless and has no AggregateFolder root with virtual children, so the user root counts 3 libraries instead of 4 and /Items?parentId=<root> returns 3. (2) PrimaryImageAspectRatio present on Jellyfin, absent on Ferrofin — FolderImageProvider generates a collage for the user root (Supports() rejects only PhotoAlbum/MusicAlbum and IsTopParent folders); Ferrofin ports CollectionFolderImageProvider (the library tiles) but not FolderImageProvider, so the user root has no Primary image. Un-defer path: port UserRootFolder's virtual-children concat (the direct-children query AND the ChildCount prefetch) and FolderImageProvider. The previous text on this row claimed the residual was '/config-vs-/data path-derived ids only'; that was measurably false — the ids were already identical on both servers and only the Path STRING differed, which is the array align key, so no field was ever compared."
  },
  "GET /Items/{itemId}/PlaybackInfo": {
   "category": "jellyfin-bug",
   "classification": "jellyfin-bug (bitrate only): Ferrofin reports MediaStreams[video].BitRate as container-minus-other-streams and MediaSources[].Bitrate as the stream sum; Jellyfin 10.11.8 falls the video stream back to formatInfo.BitRate verbatim (ProbeResultNormalizer.cs:978-987) and then InferTotalBitrate(true) (PRN.cs:259-264), which double-counts the audio. Measured on the fixture: the file is 775053 B / 1.023 s = 6,060,832 bps, so Jellyfin's MediaSources[].Bitrate of 6,253,020 exceeds what the file physically contains, while Ferrofin's 6,061,020 matches ffprobe's own format.bit_rate. Upstream fixed this itself in PR #16248 (master PRN.cs:979-991 comments that formatInfo.BitRate is the container, not the video bitrate) and Ferrofin ports the fixed form. Kept per 'don't port Jellyfin bugs'. OWNER CALL PENDING: porting 10.11.8's arithmetic instead would make this row body-diffable at the cost of being wrong. Same root cause and same batch-A3 fixes as GET /Items/{itemId}; the 'instance/probe-noise' text previously on this row was equally disproven."
  },
  "GET /Items/{itemId}/Similar": {
   "category": "instance",
   "classification": "instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93)."
  },
  "GET /Libraries/AvailableOptions": {
   "category": "expected-extension",
   "classification": "expected-extension (SupportedImageTypes + the fetcher lists): Ferrofin compiles in TheTVDB, FanArt, Open Subtitles and IntroSkipper, which the lab's stock Jellyfin 10.11.8 does not ship (GET /Plugins there lists only AudioDB and MusicBrainz). They therefore appear as extra ImageFetchers/MetadataFetchers rows, and — because SupportedImageTypes is the flattened union of every compiled image provider's GetSupportedImages (ProviderManager.cs:706-714) — their image types (Art/Disc/Banner) widen that list beyond Jellyfin's. The 2026-08-11 'verified' text this replaces was false: sweep already recorded 15 extra / 39 mismatch / 2 missing paths and schema_valid:false on this row. Fixed in batch A3: DefaultImageOptions is now the verbatim TypeOptions.DefaultImageOptions table (order included, [] for unnamed types); SupportedImageTypes is derived from the providers instead of a hardcoded 11-element enum dump; isNewLibrary now drives DefaultEnabled; and SimilarItemProviders (post-10.11.8, additionalProperties:false) is no longer serialized. OPEN WORK, not accepted: upstream's 'Screen Grabber' (VideoImageProvider, Order 100, [Primary]) is still unported — Ferrofin owns the capability (MediaEncoder::extract_video_image) but does not register the provider, so it is deliberately absent from the list rather than advertised hollow."
  },
  "GET /LiveTv/ChannelMappingOptions": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider."
  },
  "GET /LiveTv/GuideInfo": {
   "category": "instance",
   "classification": "instance: now-relative guide window (now .. now+7d) matches Jellyfin's behavior, but the exact StartDate/EndDate timestamps can't match across two independently-clocked servers."
  },
  "GET /LiveTv/ListingProviders/Lineups": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a lineup probe (needs a Schedules Direct-style provider; xmltv has none)."
  },
  "GET /LiveTv/Recordings/Groups/{groupId}": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a recordings-group probe once a recording exists."
  },
  "GET /LiveTv/SeriesTimers/{timerId}": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete)."
  },
  "GET /LiveTv/TunerHosts/Types": {
   "category": "open-work",
   "classification": "open-work (NOT accepted): Ferrofin advertises only the M3U tuner host; Jellyfin also lists HDHomeRun. Port of HdHomerunHost (~1.2K C# LOC: UDP discovery, lineup, legacy control protocol, HTTP/UDP streams) is an open work item — see brain/DEFERRED.md 2026-08-22 sweep."
  },
  "GET /MediaSegmentsApi": {
   "category": "expected-extension",
   "classification": "expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming."
  },
  "GET /Movies/{itemId}/Similar": {
   "category": "instance",
   "classification": "instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93)."
  },
  "GET /MusicGenres": {
   "category": "open-work",
   "classification": "open-work (NOT accepted) — SUPERSEDES the 2026-08-31 'jellyfin-bug' record, which was the wrong category and said so with the wrong evidence. That record folded upstream's `GroupBy(PresentationUniqueKey)` collapse under 'jellyfin-bug', which in this campaign means 'master already fixed it'. Master has NOT: master `BaseItemRepository.ByName.cs:213-224` still ships the collapse (`g.Min(e => e.Id)`, with a `TopParentIds`-preference arm for MusicArtist), and v10.11.8 `BaseItemRepository.cs:1313-1316` ships it as `g.FirstOrDefault()`. An upstream behaviour present in BOTH trees and absent from Ferrofin is an un-ported gap, so it has now been PORTED rather than labelled.\n\nWHAT F2 (as revised) FIXED — four things, all measured on the lane-2 pair:\n1. `get_genres` passed the music types as `exclude_content_types` and `get_music_genres` passed them as `include_content_types`, restricting which CONTENT items feed the aggregate. C# restricts neither: master ByName.cs:44-52 and v10.11.8 :211-222 are both `GetItemValues(filter, _getGenreValueTypes, <returnType>)`, whose inner query is built from `IncludeItemTypes` ALONE (ByName.cs:154-168); only the outer `.Where(e => e.Type == returnType)` differs. The owner-type split lives solely on the *Names* variants (`GetItemValueNames`, ByName.cs:120-141), which `get_genre_names`/`get_music_genre_names` still carry — that asymmetry IS the port. The compensation was paying for a scanner mis-split already fixed in da43f0a.\n2. `ItemCounts` were never computed (every per-kind count 0, matching neither tree). Ported MASTER's per-CleanName `BuildItemCountsByCleanName` (ByName.cs:283-338; the seven `BaseItemKindNames` locals it reads are at :304-310) as seven conditional `SUM(ci.\"Type\" = ?)` aggregates in the same GROUP BY, emitted only when the caller named item types (`if (filter.IncludeItemTypes.Length > 0)`, ByName.cs:249). `ItemCount`/`ProgramCount` stay 0 because neither tree assigns them.\n3. The representative collapse itself is now ported — `item_repository::push_representative_rank`, a `ROW_NUMBER() OVER (PARTITION BY bi.\"PresentationUniqueKey\" ORDER BY [MusicArtist TopParentId preference,] bi.\"Id\")` filtered to rank 1, run over the FILTERED row set exactly as upstream groups `masterQuery`, with the page total counting GROUPS (`representativeIds.Count()`, ByName.cs:229-232) instead of rows. Ferrofin followed master's deterministic `Min(Id)` over 10.11.8's `FirstOrDefault`. This is user-visible well beyond genres: upstream's own comment at that site says the grouping is what collapses alternate versions and a MusicArtist with a folder in more than one library.\n4. The `PresentationUniqueKey` half. The earlier record claimed 'music_genre_row now writes PresentationUniqueKey … Ferrofin writes a real {Type}-{Name} key on every one'. That was FALSE on the lab it cited: the insert is `INSERT ... WHERE NOT EXISTS`, so it never touches a row that already exists, and `MusicGenre|Ambient/Jazz/Rock` still read back NULL. Fixed properly with a startup repair, `item_persistence_service::backfill_missing_presentation_keys`, scoped to the four by-name kinds whose key is a pure function of the name. It logged `items=3` on the first boot after the change and nothing on the second, and the container's own DB now reads `MusicGenre-Ambient/-Jazz/-Rock`. Without it, porting item 3 would have COLLAPSED those three into one — reproducing upstream's collapse without reproducing upstream's keys.\n\nWHY THE ROW IS STILL RED (measured twice, 2026-08-31, after the ports above):\nH lists [Action, Ambient, Comedy, Drama, Jazz, Rock, SciFi, Thriller]; J lists [Action, Ambient, Jazz, Rock]. Two axes, and NEITHER is now an un-ported behaviour.\nAXIS 1 — Jellyfin's un-refreshed by-name keys. `CreateItemByName<T>` (master LibraryManager.cs:1289-1329 / v10.11.8 :1052-1092) constructs `new T { Name, Id, DateCreated, DateModified, Path }` and queues no refresh, so the row is written with a NULL `PresentationUniqueKey`; the key is assigned later by `MetadataService.BeforeSaveInternal` (MediaBrowser.Providers/Manager/MetadataService.cs:332-338, identical on both trees), i.e. on the row's FIRST metadata refresh. The lab's own jellyfin.db proves the mechanism exactly — every by-name row with a non-NULL DateLastRefreshed has a key and every row with a NULL one does not:\n  MusicGenre Ambient  key MusicGenre-Ambient  DateLastRefreshed 2026-08-30 00:29:31.807812\n  MusicGenre Jazz     key MusicGenre-Jazz     DateLastRefreshed 2026-08-30 00:29:31.8121208\n  MusicGenre Rock     key MusicGenre-Rock     DateLastRefreshed 2026-08-30 00:29:31.8180006\n  MusicGenre Action   key NULL                DateLastRefreshed NULL   (created 01:00:11 by a probe GET)\n  MusicGenre Comedy/Drama/SciFi/Thriller  key NULL, DateLastRefreshed NULL (created 02:20-02:21)\nSo Jellyfin's four unkeyed rows form ONE NULL group and surface as the single representative Action, while its three refreshed ones list individually. Ferrofin writes the key at creation and repairs any legacy row at startup, so it is permanently in the state Jellyfin reaches after a refresh. Reproducing J's list would mean deliberately withholding a column upstream itself fills in. On a freshly provisioned pair the probe-created rows do not exist, so this axis should not appear in the end-of-campaign sweep.\nAXIS 2 — the uncorrelated ItemCounts, a genuine jellyfin-bug that MASTER FIXED. J returns SongCount 9 for EVERY music genre and MovieCount 514 for Action, while BOTH databases hold exactly 3 Audio rows per music genre and 200 Movies carrying `action`. v10.11.8 BaseItemRepository.cs:1368-1392 hands every row the same `itemCountQuery.Count(...)` totals and comments 'TODO: This is bad refactor!'; master replaced it with the per-CleanName dictionary Ferrofin ported. Ferrofin returns each database's own truth. Following master here is the campaign rule, not a choice.\n\nNOT ACCEPTED, and nothing here is deferred: axis 1 needs no port (it is upstream transient state, with the mechanism and the evidence above) and axis 2 is master's fix. parity_diff.VOLATILE was not widened for either. deep_verified stays false, which is the honest state while the two listings genuinely differ."
  },
  "GET /Packages": {
   "category": "instance",
   "classification": "instance: aggregates the enabled repositories' manifests (PackageInfo[]) plus synthesized compiled-in entries; repositoryName/Url stamped from the repository actually fetched (manifest claims are untrusted); manifest fetch is size-capped + timed out. Catalog content is repository-config-specific — harness instances configure none — so nothing to diff. Verified 2026-08-14 by unit + server HTTP tests."
  },
  "GET /Packages/{name}": {
   "category": "instance",
   "classification": "instance: resolves a package by name/assemblyGuid from the aggregated repository catalog — catalog content is repository-config-specific, and the harness instances configure none, so there is no package to diff. Route verified 2026-08-14 by unit + server HTTP tests."
  },
  "GET /Playlists/{itemId}/InstantMix": {
   "category": "expected-extension",
   "classification": "expected-extension: Ferrofin generates an instant mix for a playlist (H=200); stock Jellyfin returns 404 (playlists are not an instant-mix source). A harmless superset."
  },
  "GET /Plugins": {
   "category": "expected-extension",
   "classification": "expected-extension: lists Ferrofin's installed plugins (compiled-in extensions + staged WASM plugins) — the set necessarily differs from any stock Jellyfin instance. Route is real and schema-conformant (server HTTP tests)."
  },
  "GET /Plugins/{pluginId}/Configuration": {
   "category": "instance",
   "classification": "instance: the two servers share no plugin id (Ferrofin ships compiled-in extensions + WASM plugins; stock Jellyfin ships neither), so there is no config to fetch on both sides. Real read (stored values overlaid on defaults, C# partial-deserialize semantics); verified 2026-08-14 by unit + handler tests."
  },
  "GET /Plugins/{pluginId}/{version}/Image": {
   "category": "instance",
   "classification": "instance: no shared plugin id across the two servers, so no image to fetch on both sides. Real read (bundled image bytes + content type); verified 2026-08-14 by handler tests."
  },
  "GET /Providers/Lyrics/{lyricId}": {
   "category": "expected-extension",
   "classification": "expected-extension: undiffable by construction. Jellyfin 10.11.8 registers no lyric provider, so `InternalGetRemoteLyricsAsync` always hits `GetProvider(...) is null` and this route answers 404 for EVERY possible id, where Ferrofin's LrcLib returns a real 200. The ids are not portable either — Ferrofin's provider-local id is `{lrclib_id}_{synced|plain}`, a shape no Jellyfin provider would mint. What the two servers CAN agree on is the refusal, and they now do: a bogus id and an id with no variant suffix are both 404/404 (the latter used to be an off-contract 400 from LrcLibProvider::get_lyrics; the vendored contract declares only 200/404, and the C# has no 400 path). The parse behind the 200 is pinned by the GET /Audio/{itemId}/Lyrics body diff."
  },
  "GET /Providers/Subtitles/Subtitles/{subtitleId}": {
   "category": "remote-opt-in",
   "classification": "remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live."
  },
  "GET /Repositories": {
   "category": "instance",
   "classification": "instance: returns the admin-configured package repositories — instance-local state (harness instances configure none), nothing to diff. Verified 2026-08-14 by unit + handler tests."
  },
  "GET /ScheduledTasks": {
   "category": "instance",
   "classification": "instance: LastExecutionResult depends on whether/when a task last ran."
  },
  "GET /Shows/{itemId}/Similar": {
   "category": "instance",
   "classification": "flat-provider methodology (SET-GATED, order not gateable). Seeds of this kind take the C# FLAT provider: one `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags, IncludeItemTypes = [<kind>], EnableGroupByMetadataKey = false, EnableTotalRecordCount = true, OrderBy = [(ItemSortBy.Random, SortOrder.Ascending)] }` — identical in v10.11.8 (built inline at LibraryController.cs:791-801) and in master (SeriesSimilarItemsProvider.cs:49, MusicAlbumSimilarItemsProvider.cs:50, MusicArtistSimilarItemsProvider.cs:50, AudioSimilarItemsProvider.cs:50, LiveTvProgramSimilarItemsProvider.cs:89). Ferrofin runs the same query and, as of this pass, the same `Random` order. IT BRIEFLY DID NOT: batch A2 first shipped `SortName` here, which is not cosmetic — at limit < |candidates| a fixed order returns a DIFFERENT ITEM, measured on the loop pair as `/Items/{audio}/Similar?limit=1` answering 'Artist 01 Album 01 Track 02' on five consecutive Ferrofin calls while Jellyfin alternated Track 02 / Track 03. That divergence is now closed in the server (`similar_items_manager.rs::flat_similar`, unit test `the_flat_provider_orders_random_not_by_name`), not papered over. WHAT THIS LEDGER ROW DOES AND DOES NOT GATE: `Random` on both sides means the ORDER can never be diffed by any probe, and neither can a page narrower than the candidate pool — parity_diff aligns arrays by Path>Name>Id, so a reordered Items array returns 0 diffs and always will. The reads.py leg therefore pins `limit=1000`, far above the fixture's pool, where the answer is the whole candidate set and so fully determined; what is gated there is the SET, TotalRecordCount and every per-item DTO field. Anything order-sensitive is covered by unit tests instead, never by this row. SERIES SEED: the pre-A2 server ran the weighted MOVIE scorer for every kind, which credits a shared studio (+5) and shared people and pulled genre-unrelated series in (F ['Series 06','Series 03','Series 05','Series 07'] vs J ['Series 06']); `SeriesSimilarItemsProvider` filters on Genres/Tags alone, and porting it made the set match. The probe also exposed a DTO gap since fixed: Ferrofin omitted `AirDays` on Series where 10.11.8 always sends `[]` (v10.11.8 DtoService.cs:1243 assigns `series.AirDays`, and v10.11.8 Series.cs:31 initialises it to `Array.Empty<DayOfWeek>()` while no column persists it) — the null-where-Jellyfin-sends-non-null shape strict clients crash on."
  },
  "GET /Studios/{name}": {
   "category": "open-work",
   "classification": "open-work (NOT accepted) — SUPERSEDES the batch-B1 record. B1's two claims still hold and were re-measured on the current tree: the `CreateItemByName<Studio>` lazy create is live (`/Studios/<never-seen name>` is 200 on both, with the Jellyfin-shaped Path), and the per-kind ItemValue count fix holds. Its closing sentence, 'the REAL studios still count 254/250/4 on both servers', was a hand measurement no layer reproduced; reads.py now carries a THIRD leg over the LAST shared studio by SortName, so the claim is probed. Current state: 2/3 legs clean.\n\nWHAT F2 FIXED (a real server bug the third leg exists to keep honest): a provider refresh updated `BaseItems.Studios/Genres/Tags` but never re-derived `ItemValues`/`ItemValuesMap`, so the by-name counts this route returns went stale against Ferrofin's OWN corpus. `ProviderManager::persist_refreshed` called only `save_items`; C# does both halves in one call — `BaseItemRepository.SaveItems` saves the row and then rewrites the by-name index for it (v10.11.8 `BaseItemRepository.cs:674-735`, unchanged on master), reached from a refresh via `MetadataService.SaveItemAsync` → `UpdateToRepositoryAsync` → `LibraryManager.UpdateItemAsync`. Ferrofin's editor path (`LibraryManager::update_items`) already did this; the refresh path was the hole. Fixed by calling `save_item_values(id, &item_values_of(entity))` there; `item_values_of` moved to `ferrofin-db` so `ferrofin-providers` can use it without depending on `ferrofin-core`. Damage already on disk is still visible on this lab (H's Movie 0001 row says Studios='Castle Rock Entertainment' while its ItemValuesMap still says 'Ferrofin Studios') and is repaired by the next library scan, which re-derives the index per item — unlike the Person columns, this index has a self-heal path, so no startup repair was added.\n\nWHY THE ROW IS STILL RED, and it is NOT this fix: the remaining leg diffs ChildCount J=253 H=254 / MovieCount J=249 H=250 on 'Parity Pictures', and that is measured CORPUS SKEW from an earlier lane's write journey, not a query defect. Each server's answer is exactly consistent with its own database (H 250 movies carry 'parity pictures', J 249). The single divergent item is Movie 0002: its on-disk NFO says `<studio>Parity Pictures</studio>`, H's row carries Studios='Parity Pictures', Genres='Comedy|SciFi', Tags='parity-test', and J's row carries NONE of the three — Jellyfin lost them. Cleared by the campaign's wipe-and-provision sweep; NOT chased here, because resetting one side of a shared pair is exactly what the lab-hygiene rule forbids.\n\nPROBE NOTE (review pass): the third leg is the LAST shared studio by SortName. When fewer than two names are shared it is now SKIPPED, not silently re-pointed at the first shared name — the old fallback made the extra leg a byte-identical duplicate and doubled the `field(s) compared` depth the ledger publishes for no extra coverage. It does not fire on this lab (both studios are shared), but a corpus that narrows should shrink the evidence, not inflate it."
  },
  "GET /System/ActivityLog/Entries": {
   "category": "instance",
   "classification": "instance: activity-log entries differ per instance/run."
  },
  "GET /System/Info/Storage": {
   "category": "instance",
   "classification": "instance: per-host disk paths and free/used space."
  },
  "GET /System/Logs": {
   "category": "instance",
   "classification": "instance: log-file set differs per instance/run."
  },
  "GET /System/Ping": {
   "category": "instance",
   "classification": "instance: returns the server name string (ferrofin vs the Jellyfin instance name)."
  },
  "GET /Trailers": {
   "category": "instance",
   "classification": "instance: Jellyfin 10.11.8 returns 500 here (its own error with no Trailers provider), while Ferrofin correctly returns 200 with an empty result. Kept correct per policy (do not port Jellyfin bugs); not a Ferrofin gap."
  },
  "GET /Trailers/{itemId}/Similar": {
   "category": "instance",
   "classification": "instance/methodology (PROPERTY-VERIFIED, NOT body-diffed — the ledger counts and lists this row separately from the deep-verified ones; status codes, the empty short-circuits, the count invariant and the candidate rule are what is gated, by the reads.py `similar_invariants` probe, and each of the three property rows now issues its OWN alias rather than all three probing /Movies). Two independent reasons a movie seed's item array cannot diff, both measured on the loop pair. (1) ORDER: Jellyfin 10.11.8 sets `OrderBy = [(ItemSortBy.Random, Ascending)]` (v10.11.8 LibraryController.cs:801); five identical calls at limit=12 returned five different SETS, because limit < |universe|. (2) ALGORITHM: 10.11.8 builds ONE flat `InternalItemsQuery { Genres = item.Genres, Tags = item.Tags }`, while Ferrofin ports upstream master's weighted `MovieSimilarItemsProvider` (genre 10 / tag 5 / studio 5 / director 50 / actor+guest-star 15, MovieSimilarItemsProvider.cs:27-31) plus its phase-2 `IsMovie = true, IsPlayed = false` + ConfigureUserAccess pass. Measured at limit=1000: Jellyfin 299 items, Ferrofin 432, and Jellyfin's set is a STRICT SUBSET of Ferrofin's (onlyJ=0); the 133 extras each share a studio and/or a person with the seed and no genre — master's dimensions exactly. *** OWNER DECISION PENDING (NOT an agent-granted acceptance): running master's scorer against a 10.11.8 oracle is a deliberate ALGORITHM divergence on three ops, and CLAUDE.md reserves additions to the accepted-divergence list to the owner. The engineering case for keeping master is the repo's 'do not port Jellyfin bugs' rule — 10.11.8 additionally over-returns by 4 at every limit (measured 1/3/5/12/50 -> 5/7/9/16/54) because `BaseItemRepository.PrepareFilterQuery` (v10.11.8:1427-1430) adds 4 when `EnableGroupByMetadataKey` and never trims back, while Ferrofin honours limit exactly as master's `.Take(requestedLimit)` does — but the call is the owner's, and reverting to 10.11.8's flat query would make these three rows body-diffable at a pinned wide limit. *** WHAT IS VERIFIED LIVE instead of a body diff (every fact computed per server and then compared): status 200, StartIndex 0, TotalRecordCount == len(Items), the seed is never returned, every Type is in the C# IncludeItemTypes, the page is full, len(Items) <= limit+4 (the named Jellyfin quirk is the ONLY slack), an unknown seed is 404 on both, a nil seed is 200-empty on both, an Episode seed short-circuits empty on both, the five aliases agree as sets at limit=1000, and `obeys_own_candidate_rule` — each server's answer is re-derived independently from the DTO fields under ITS OWN documented algorithm (Jellyfin: every row shares a genre or tag with the seed, which 200 of the 500 fixture movies violate; Ferrofin: the answer equals master's weighted model's top-N IN ORDER by score desc, then Ferrofin's OWN SortName/Id tiebreak — master has no tiebreak at all, `MovieSimilarItemsProvider.cs:189-195` is `OrderByDescending(score)` alone, so that clause pins Ferrofin's rule, which is strictly more determined than the C#'s). The person dimension is matched as (Name, PersonType) pairs because that is what a `PeopleId` IS — 'the Peoples table has one row per (Name, PersonType)' (PeopleRepository.cs:47) — which is exactly what both master's `PeopleId` join and Ferrofin's SQL compute. Negative controls run 2026-08-29: the Ferrofin clause fails on a reversed order and on 12 arbitrary movies, the Jellyfin clause fails on 12 movies outside its universe. NOT REACHED BY ANY PARITY LAYER, and unit-covered instead: the phase-2 filters — the lab has zero played movies and one all-libraries user, so `IsPlayed = false` and ConfigureUserAccess are exercised only by `a_played_movie_is_not_suggested_to_the_user_who_played_it`, `a_library_the_user_cannot_access_contributes_nothing`, `a_blocked_media_folder_hides_its_rows` and `the_access_filter_does_not_starve_the_page` in similar_items_manager.rs. Three real Ferrofin bugs this row used to hide were fixed in the same pass: an unknown seed returned 200-empty instead of 404, a nil seed returned 400 instead of the C# root-folder fallback, and the no-limit default was 10 instead of C#'s `limit ?? 50` (SimilarItemsManager.cs:93)."
  },
  "GET /Years": {
   "category": "instance",
   "classification": "instance/methodology — Ferrofin is the more-correct side. SUPERSEDES the earlier \"expected-extension: Ferrofin does not materialize Year items (documented out-of-scope)\" record, which is FALSE on the current tree (`YearStore` materializes a Year item per distinct ProductionYear and /Years returns them) and which used wording this project does not accept. The divergence that actually remains is Jellyfin's TotalRecordCount, not the items: `YearsController.GetYears` takes `totalCount` from `folder.GetRecursiveChildren(user, query, out totalCount)` (YearsController.cs:122) — the number of ITEMS scanned — and returns it beside the year DTOs (YearsController.cs:156-158). Measured on the lane-2 pair: J TotalRecordCount=559 with 3 Items; Ferrofin returns 3 with the same 3 Items (2020/2021/2022, identical bodies). Kept correct per the project's don't-port-Jellyfin-bugs rule."
  },
  "GET /Years/{year}": {
   "category": "verified",
   "classification": "verified 2026-08-29 (batch B1, reviewer round 2). SUPERSEDES \"expected-extension: Ferrofin does not materialize Year items (documented out-of-scope)\" — false on the current tree, and not an answer this project accepts. Year items ARE materialized (`YearStore`) and the route 200s. A SECOND defect was found and fixed in this pass, and it is recorded rather than silently closed: `ItemCountService.cs:170-181` counts a Year by `e.ProductionYear == year` and never touches ItemValues, while Ferrofin routed Year through the clean-value aggregate — so every year reported ChildCount 0. Measured before: /Years/2020 H 0 vs J 500. After the port: /Years/2020 500/500, /Years/2021 8 series, /Years/2022 12, /Years/2001 0, diffs=0 on all four. Now probed by reads.py as its own row (`GET /Years/{year}`), which it never was."
  },
  "GET /web/ConfigurationPage": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "GET /web/ConfigurationPages": {
   "category": "expected-extension",
   "classification": "expected-extension: plugin configuration pages come from external plugins Ferrofin doesn't host."
  },
  "POST /Audio/{itemId}/RemoteSearch/Lyrics/{lyricId}": {
   "category": "expected-extension",
   "classification": "expected-extension: the download half of the same asymmetry. With no ILyricProvider registered, `LyricManager.DownloadLyricsAsync` short-circuits on `GetProvider(lyricId.LeftPart('_')) is null` and the route can only ever 404 on Jellyfin 10.11.8, while Ferrofin's LrcLib really fetches. The only cross-server-comparable observation is the refusal, and it agrees: a bogus lyric id is 404/404 and a non-audio itemId is 404/404 (measured on the lab pair, twice). The success leg exists on one server only, so this row cannot earn a body diff — but the LyricDto it returns is the same parse that `GET /Audio/{itemId}/Lyrics` now body-diffs over identical uploaded bytes."
  },
  "POST /Episode/{Id}/Timestamps": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /FileTransformation/RegisterTransformation": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Intros/AnalyzerActions/UpdateSeason": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Intros/EraseTimestamps": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Intros/RebuildDatabase": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Intros/ScanSeason/{SeriesId}/{SeasonId}": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Items/RemoteSearch/BoxSet": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (a NARROW input class; the op itself is deep-verified — j_remote_search_identify diffs the FULL response body, key for key, on every other case). A NON-NUMERIC `Tmdb` provider id crashes the oracle's provider: v10.11.8 TmdbBoxSetProvider.cs:44 `Convert.ToInt32(searchInfo.GetProviderId(MetadataProvider.Tmdb), CultureInfo.InvariantCulture)` parses the stored id unguarded, so \"notanumber\" throws a FormatException, ProviderManager.GetRemoteSearchResults' catch-all swallows it, and Jellyfin answers [] — the name search the user actually typed never runs. Ferrofin declines to port the crash: a value that is not a number is not an id, so the remaining branches (and finally the name search) run. Per the repo's \"don't port Jellyfin bugs\" practice, Ferrofin is the correct side. Measured twice on the C1 lab pair with {\"ProviderIds\":{\"Tmdb\":\"notanumber\"},\"Name\":\"<a real title>\"}: Ferrofin 200 with 3 /search/collection rows for Name=\"The Lord of the Rings Collection\", Jellyfin 200 []. NOT to be confused with a WHITESPACE-PADDED id: .NET's `int.Parse`/`Convert.ToInt32` default to NumberStyles.Integer (AllowLeadingWhite|AllowTrailingWhite), so \" 603 \" DOES pin the title upstream. That one was a real Ferrofin bug (`str::parse` rejects the padding, so the id branch was skipped and Identify answered with the name search); it is fixed — provider_manager.rs `parse_numeric_provider_id` trims — and is now probed live by the `*_padded` cases in j_remote_search_identify. Un-classify only if upstream starts guarding the parse."
  },
  "POST /Items/RemoteSearch/Movie": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (a NARROW input class; the op itself is deep-verified — j_remote_search_identify diffs the FULL response body, key for key, on every other case). A NON-NUMERIC `Tmdb` provider id crashes the oracle's provider: v10.11.8 TmdbMovieProvider.cs:59 `int.Parse(id, CultureInfo.InvariantCulture)` parses the stored id unguarded, so \"notanumber\" throws a FormatException, ProviderManager.GetRemoteSearchResults' catch-all swallows it, and Jellyfin answers [] — the name search the user actually typed never runs. Ferrofin declines to port the crash: a value that is not a number is not an id, so the remaining branches (and finally the name search) run. Per the repo's \"don't port Jellyfin bugs\" practice, Ferrofin is the correct side. Measured twice on the C1 lab pair with {\"ProviderIds\":{\"Tmdb\":\"notanumber\"},\"Name\":\"<a real title>\"}: Ferrofin 200 with 20 /search/movie rows for Name=\"The Matrix\", Jellyfin 200 []. NOT to be confused with a WHITESPACE-PADDED id: .NET's `int.Parse`/`Convert.ToInt32` default to NumberStyles.Integer (AllowLeadingWhite|AllowTrailingWhite), so \" 603 \" DOES pin the title upstream. That one was a real Ferrofin bug (`str::parse` rejects the padding, so the id branch was skipped and Identify answered with the name search); it is fixed — provider_manager.rs `parse_numeric_provider_id` trims — and is now probed live by the `*_padded` cases in j_remote_search_identify. Un-classify only if upstream starts guarding the parse."
  },
  "POST /Items/RemoteSearch/Person": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (a NARROW input class; the op itself is deep-verified — j_remote_search_identify diffs the FULL response body, key for key, on every other case). A NON-NUMERIC `Tmdb` provider id crashes the oracle's provider: v10.11.8 TmdbPersonProvider.cs `int.Parse(id, CultureInfo.InvariantCulture)` parses the stored id unguarded, so \"notanumber\" throws a FormatException, ProviderManager.GetRemoteSearchResults' catch-all swallows it, and Jellyfin answers [] — the name search the user actually typed never runs. Ferrofin declines to port the crash: a value that is not a number is not an id, so the remaining branches (and finally the name search) run. Per the repo's \"don't port Jellyfin bugs\" practice, Ferrofin is the correct side. Measured twice on the C1 lab pair with {\"ProviderIds\":{\"Tmdb\":\"notanumber\"},\"Name\":\"<a real title>\"}: Ferrofin 200 with 1 /search/person row for Name=\"Tom Hanks\", Jellyfin 200 []. NOT to be confused with a WHITESPACE-PADDED id: .NET's `int.Parse`/`Convert.ToInt32` default to NumberStyles.Integer (AllowLeadingWhite|AllowTrailingWhite), so \" 603 \" DOES pin the title upstream. That one was a real Ferrofin bug (`str::parse` rejects the padding, so the id branch was skipped and Identify answered with the name search); it is fixed — provider_manager.rs `parse_numeric_provider_id` trims — and is now probed live by the `*_padded` cases in j_remote_search_identify. Un-classify only if upstream starts guarding the parse."
  },
  "POST /Items/RemoteSearch/Series": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (a NARROW input class; the op itself is deep-verified — j_remote_search_identify diffs the FULL response body, key for key, on every other case). A NON-NUMERIC `Tmdb` provider id crashes the oracle's provider: v10.11.8 TmdbSeriesProvider.cs:58 `Convert.ToInt32(tmdbId, CultureInfo.InvariantCulture)` parses the stored id unguarded, so \"notanumber\" throws a FormatException, ProviderManager.GetRemoteSearchResults' catch-all swallows it, and Jellyfin answers [] — the name search the user actually typed never runs. Ferrofin declines to port the crash: a value that is not a number is not an id, so the remaining branches (and finally the name search) run. Per the repo's \"don't port Jellyfin bugs\" practice, Ferrofin is the correct side. Measured twice on the C1 lab pair with {\"ProviderIds\":{\"Tmdb\":\"notanumber\"},\"Name\":\"<a real title>\"}: Ferrofin 200 with 4 /search/tv rows for Name=\"Breaking Bad\", Jellyfin 200 []. NOT to be confused with a WHITESPACE-PADDED id: .NET's `int.Parse`/`Convert.ToInt32` default to NumberStyles.Integer (AllowLeadingWhite|AllowTrailingWhite), so \" 603 \" DOES pin the title upstream. That one was a real Ferrofin bug (`str::parse` rejects the padding, so the id branch was skipped and Identify answered with the name search); it is fixed — provider_manager.rs `parse_numeric_provider_id` trims — and is now probed live by the `*_padded` cases in j_remote_search_identify. Un-classify only if upstream starts guarding the parse."
  },
  "POST /Items/RemoteSearch/Trailer": {
   "category": "remote-opt-in",
   "classification": "remote-opt-in: OMDb is the ONLY provider implementing IRemoteMetadataProvider<Trailer, TrailerInfo> upstream (v10.11.8: `git grep \"Trailer, TrailerInfo\"` matches only MediaBrowser.Providers/Plugins/Omdb/OmdbItemProvider.cs:31 plus ItemLookupController), and Jellyfin ships a hardcoded key — OmdbProvider.cs:257 `const string Url = \"https://www.omdbapi.com?apikey=2c9d9507\";`. Ferrofin's OMDb client is inert until FERROFIN_OMDB_KEY / config `omdb_api_key` is set (the owner-listed scope divergence in CLAUDE.md \"Current scope\"; omdb.rs `enabled()` is `!api_key.is_empty()`), so a keyless Ferrofin answers [] where the oracle answers 6 rows. Measured twice on the C1 lab pair: F 200 [] / J 200 six results for {\"Name\":\"Inception\",\"Year\":2010}; the bogus-term case is 200 [] on both. NOT a routing, auth or DTO gap — the same body against /Items/RemoteSearch/Movie returns matching TMDB results on both, and curl from inside the Ferrofin container reaches omdbapi.com fine. The two real port gaps this row would otherwise have surfaced ARE fixed and unit-tested (the index-number echo of OmdbItemProvider.cs:185-192, and the `_libraryManager.ParseName` pass at :110-117 that lifts the year out of the title); they cannot be observed live until the lab's Ferrofin service is given an OMDb key. Un-classify by setting FERROFIN_OMDB_KEY on the Ferrofin service only and re-running the j_remote_search_identify journey, which already probes this op on both servers."
  },
  "POST /Items/{itemId}/RemoteSearch/Subtitles/{subtitleId}": {
   "category": "remote-opt-in",
   "classification": "remote-opt-in: needs an opensubtitles.com account and the public internet. With OPENSUBTITLES_USERNAME/PASSWORD/API_KEY set, the parity leg installs the plugin on the oracle, configures both, and the remote-subtitle journey (search -> download -> stream read-back -> provider file) runs live."
  },
  "POST /Jellyfin.Plugin.OpenSubtitles/ValidateLoginInfo": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Library/Movies/Added": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (the SELECTOR half; the refresh half is confirmed live on Ferrofin by j_library_webhooks — create by path, id-matched NFO refresh, unmatched-id negative control). Jellyfin 10.11.8 cannot match an external id on these four routes AT ALL: LibraryController.PostUpdatedSeries / PostUpdatedMovies (v10.11.8 Jellyfin.Api/Controllers/LibraryController.cs:571-635) query with `DtoOptions(false)`, whose ctor sets `Fields = []` (MediaBrowser.Controller/Dto/DtoOptions.cs:29-37), so BaseItemRepository.ApplyNavigations skips `Include(e => e.Provider)` (Jellyfin.Server.Implementations/Item/BaseItemRepository.cs:442-444) and DeserializeBaseItem never fills ProviderIds (:895-897) — the in-memory `string.Equals(id, i.GetProviderId(...))` filter therefore compares every item against null. Measured twice on the C4 lab pair 2026-08-30 (F=18296, J=18297): (a) a supplied id matches NOTHING even when the item demonstrably carries it — Jellyfin's own DTO reports Movie 0001 as ProviderIds {Imdb: tt0111161} and `POST /Library/Movies/Updated?imdbId=tt0111161` produced no `will be refreshed` line in 20 s; a probe series carrying Tvdb 990002 was still at its old Overview 60 s after `?tvdbId=990002`, and the same probe pair run through the journey leaves all four rows H=True J=False. (b) with the id OMITTED, `string.Equals(null, null)` is TRUE, so a bare `POST /Library/Series/Added` reported EVERY series (logged: the probe plus Series 01-08 'will be refreshed' in one burst) — a whole-TV-library refresh from an empty request. Ferrofin pushes the id into the SQL query instead (`paths_by_provider_id` → InternalItemsQuery::any_provider_id_equals, crates/ferrofin-api/src/handlers/library.rs), so a real id matches and an absent id is a no-op: the route's documented intent, kept under the repo's 'don't port Jellyfin bugs' rule. These rows therefore cannot be deep_verified against this oracle; the journey records Ferrofin's effect and Jellyfin's silence rather than a green."
  },
  "POST /Library/Movies/Updated": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (the SELECTOR half; the refresh half is confirmed live on Ferrofin by j_library_webhooks — create by path, id-matched NFO refresh, unmatched-id negative control). Jellyfin 10.11.8 cannot match an external id on these four routes AT ALL: LibraryController.PostUpdatedSeries / PostUpdatedMovies (v10.11.8 Jellyfin.Api/Controllers/LibraryController.cs:571-635) query with `DtoOptions(false)`, whose ctor sets `Fields = []` (MediaBrowser.Controller/Dto/DtoOptions.cs:29-37), so BaseItemRepository.ApplyNavigations skips `Include(e => e.Provider)` (Jellyfin.Server.Implementations/Item/BaseItemRepository.cs:442-444) and DeserializeBaseItem never fills ProviderIds (:895-897) — the in-memory `string.Equals(id, i.GetProviderId(...))` filter therefore compares every item against null. Measured twice on the C4 lab pair 2026-08-30 (F=18296, J=18297): (a) a supplied id matches NOTHING even when the item demonstrably carries it — Jellyfin's own DTO reports Movie 0001 as ProviderIds {Imdb: tt0111161} and `POST /Library/Movies/Updated?imdbId=tt0111161` produced no `will be refreshed` line in 20 s; a probe series carrying Tvdb 990002 was still at its old Overview 60 s after `?tvdbId=990002`, and the same probe pair run through the journey leaves all four rows H=True J=False. (b) with the id OMITTED, `string.Equals(null, null)` is TRUE, so a bare `POST /Library/Series/Added` reported EVERY series (logged: the probe plus Series 01-08 'will be refreshed' in one burst) — a whole-TV-library refresh from an empty request. Ferrofin pushes the id into the SQL query instead (`paths_by_provider_id` → InternalItemsQuery::any_provider_id_equals, crates/ferrofin-api/src/handlers/library.rs), so a real id matches and an absent id is a no-op: the route's documented intent, kept under the repo's 'don't port Jellyfin bugs' rule. These rows therefore cannot be deep_verified against this oracle; the journey records Ferrofin's effect and Jellyfin's silence rather than a green."
  },
  "POST /Library/Series/Added": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (the SELECTOR half; the refresh half is confirmed live on Ferrofin by j_library_webhooks — create by path, id-matched NFO refresh, unmatched-id negative control). Jellyfin 10.11.8 cannot match an external id on these four routes AT ALL: LibraryController.PostUpdatedSeries / PostUpdatedMovies (v10.11.8 Jellyfin.Api/Controllers/LibraryController.cs:571-635) query with `DtoOptions(false)`, whose ctor sets `Fields = []` (MediaBrowser.Controller/Dto/DtoOptions.cs:29-37), so BaseItemRepository.ApplyNavigations skips `Include(e => e.Provider)` (Jellyfin.Server.Implementations/Item/BaseItemRepository.cs:442-444) and DeserializeBaseItem never fills ProviderIds (:895-897) — the in-memory `string.Equals(id, i.GetProviderId(...))` filter therefore compares every item against null. Measured twice on the C4 lab pair 2026-08-30 (F=18296, J=18297): (a) a supplied id matches NOTHING even when the item demonstrably carries it — Jellyfin's own DTO reports Movie 0001 as ProviderIds {Imdb: tt0111161} and `POST /Library/Movies/Updated?imdbId=tt0111161` produced no `will be refreshed` line in 20 s; a probe series carrying Tvdb 990002 was still at its old Overview 60 s after `?tvdbId=990002`, and the same probe pair run through the journey leaves all four rows H=True J=False. (b) with the id OMITTED, `string.Equals(null, null)` is TRUE, so a bare `POST /Library/Series/Added` reported EVERY series (logged: the probe plus Series 01-08 'will be refreshed' in one burst) — a whole-TV-library refresh from an empty request. Ferrofin pushes the id into the SQL query instead (`paths_by_provider_id` → InternalItemsQuery::any_provider_id_equals, crates/ferrofin-api/src/handlers/library.rs), so a real id matches and an absent id is a no-op: the route's documented intent, kept under the repo's 'don't port Jellyfin bugs' rule. These rows therefore cannot be deep_verified against this oracle; the journey records Ferrofin's effect and Jellyfin's silence rather than a green."
  },
  "POST /Library/Series/Updated": {
   "category": "jellyfin-bug-not-ported",
   "classification": "jellyfin-bug-not-ported (the SELECTOR half; the refresh half is confirmed live on Ferrofin by j_library_webhooks — create by path, id-matched NFO refresh, unmatched-id negative control). Jellyfin 10.11.8 cannot match an external id on these four routes AT ALL: LibraryController.PostUpdatedSeries / PostUpdatedMovies (v10.11.8 Jellyfin.Api/Controllers/LibraryController.cs:571-635) query with `DtoOptions(false)`, whose ctor sets `Fields = []` (MediaBrowser.Controller/Dto/DtoOptions.cs:29-37), so BaseItemRepository.ApplyNavigations skips `Include(e => e.Provider)` (Jellyfin.Server.Implementations/Item/BaseItemRepository.cs:442-444) and DeserializeBaseItem never fills ProviderIds (:895-897) — the in-memory `string.Equals(id, i.GetProviderId(...))` filter therefore compares every item against null. Measured twice on the C4 lab pair 2026-08-30 (F=18296, J=18297): (a) a supplied id matches NOTHING even when the item demonstrably carries it — Jellyfin's own DTO reports Movie 0001 as ProviderIds {Imdb: tt0111161} and `POST /Library/Movies/Updated?imdbId=tt0111161` produced no `will be refreshed` line in 20 s; a probe series carrying Tvdb 990002 was still at its old Overview 60 s after `?tvdbId=990002`, and the same probe pair run through the journey leaves all four rows H=True J=False. (b) with the id OMITTED, `string.Equals(null, null)` is TRUE, so a bare `POST /Library/Series/Added` reported EVERY series (logged: the probe plus Series 01-08 'will be refreshed' in one burst) — a whole-TV-library refresh from an empty request. Ferrofin pushes the id into the SQL query instead (`paths_by_provider_id` → InternalItemsQuery::any_provider_id_equals, crates/ferrofin-api/src/handlers/library.rs), so a real id matches and an absent id is a no-op: the route's documented intent, kept under the repo's 'don't port Jellyfin bugs' rule. These rows therefore cannot be deep_verified against this oracle; the journey records Ferrofin's effect and Jellyfin's silence rather than a green."
  },
  "POST /LiveTv/ChannelMappings": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a channel-mapping probe against the fixture's listings provider."
  },
  "POST /LiveTv/Programs": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a programmes-by-body probe (the POST form of /LiveTv/Programs)."
  },
  "POST /LiveTv/SeriesTimers": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete)."
  },
  "POST /LiveTv/SeriesTimers/{timerId}": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a series-timer journey (create from a programme → list → delete)."
  },
  "POST /LiveTv/Timers/{timerId}": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs an update step in the timer journey (change pre-padding, read back)."
  },
  "POST /LiveTv/Tuners/{tunerId}/Reset": {
   "category": "requires-livetv-tuner",
   "classification": "requires-livetv-tuner: the fixture tuner (M3U + XMLTV, two channels) exists, but no probe exercises this op yet — needs a tuner-reset probe with the fixture tuner's id."
  },
  "POST /MediaSegmentsApi/{itemId}": {
   "category": "expected-extension",
   "classification": "expected-extension: MediaSegmentsApi is a compiled-in Ferrofin plugin; stock Jellyfin 10.11.8 lacks it (J=404 without the plugin installed). JSON API, not streaming."
  },
  "POST /MergeVersions/MergeEpisodes": {
   "category": "expected-extension",
   "classification": "expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design."
  },
  "POST /MergeVersions/MergeMovies": {
   "category": "expected-extension",
   "classification": "expected-extension: Ferrofin ships the MergeVersions controller (danieladov 'Merge Versions' plugin surface) compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design."
  },
  "POST /MergeVersions/SplitEpisodes": {
   "category": "expected-extension",
   "classification": "expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design."
  },
  "POST /MergeVersions/SplitMovies": {
   "category": "expected-extension",
   "classification": "expected-extension: Ferrofin ships the MergeVersions controller compiled-in; stock Jellyfin 10.11.8 lacks it (H=2xx vs J=404). By design."
  },
  "POST /Packages/Installed/{name}": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: repository install of WASM plugins is real (2026-08-14: ABI gate → HTTPS-per-hop download → streamed size cap → sha256/md5 verify → component+identity validation → guid-squat refusal → atomic stage → restart-required; admin-only; end-to-end server test). Divergence vs Jellyfin: the install is SYNCHRONOUS — none of the PackageInstalling/Completed/Failed/Cancelled WebSocket messages are emitted (no dashboard progress; a slow repo holds the POST open), and the plugin appears in /Plugins only after the activating restart. Async install + Package* push is the recorded follow-up (WebSocket-push backlog)."
  },
  "POST /Plugins/{pluginId}/Configuration": {
   "category": "instance",
   "classification": "instance: no shared plugin id across the two servers, so no config write to diff. Real write (JSON-validated, persisted; admin-only — a WASM plugin's config is guest input); verified 2026-08-14 by unit + handler tests incl. 403."
  },
  "POST /Plugins/{pluginId}/Manifest": {
   "category": "instance",
   "classification": "instance: no shared plugin id across the two servers, so no manifest to fetch on both sides. Real read (GetPluginManifest port); verified 2026-08-14 by handler tests."
  },
  "POST /Plugins/{pluginId}/{version}/Disable": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window."
  },
  "POST /Plugins/{pluginId}/{version}/Enable": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: real enabled-flag toggle (admin-only), but a freshly repository-installed WASM plugin 404s until its activating restart (not in the registry until the boot that loads it) while Jellyfin already lists it restart-pending. Cosmetic pre-restart window."
  },
  "POST /Repositories": {
   "category": "instance",
   "classification": "instance: replaces the configured package repositories (admin-only, RequiresElevation port) — instance-local state, nothing to diff. Verified 2026-08-14 by unit + handler tests incl. 403 for non-admin."
  },
  "POST /SkipButtonCss/InjectCss": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /SkipButtonCss/UpdateSkipDuration": {
   "category": "expected-extension",
   "classification": "expected-extension: compiled-in/extension route not on the stock Jellyfin surface the fixture exercises."
  },
  "POST /Startup/User": {
   "category": "accepted-divergence",
   "classification": "accepted-divergence: post-setup the first user already has a password and the contract is 403 (the Forbid guard upstream added in v12, ported by Ferrofin); Jellyfin 10.11.8 predates it and answers 204, silently re-setting the admin password. The journey asserts the correct contract (H=403 J=204)."
  },
  "POST /Items/RemoteSearch/MusicArtist": {
   "category": "verified",
   "classification": "The library metadata-fetcher gate is now PORTED on this path: `LocalProviderManager::remote_search` resolves the `ItemId` reference item, reads ITS library's TypeOptions and drops every remote fetcher the \"Metadata downloaders\" list leaves unticked, honouring `IncludeDisabledProviders` as the short-circuit and the library's `MetadataFetcherOrder` for ordering — a port of MediaBrowser.Providers/Manager/ProviderManager.cs:787-830 -> :440-460 -> :462-491 -> MediaBrowser.Controller/BaseItemManager/BaseItemManager.cs:26-46. It only became REACHABLE for MusicArtist once `MusicArtistResolver` landed, because the gate resolves a library through `TopParentId` and an accessed-by-name artist has none. Measured live on the lab pair, both servers: scoped search with `ItemId` -> 200 with 0 results on BOTH (the fixture library ticks no MusicArtist fetcher; Ferrofin used to return 25), and `IncludeDisabledProviders=true` puts MusicBrainz back. The row also gained its first probe — `j_remote_search_identify` now sends `ItemId` and `IncludeDisabledProviders`, which no case did before, and resolves the artist id PER SERVER by Path so a server with no resolvable artist FAILS the row rather than skipping it. MEASUREMENT CAVEAT, not a divergence: MusicBrainz rate-limits both containers behind one egress IP and answers an over-rate query with an empty list, so the probe paces its legs 6 s apart and retries 5 times; if both servers still come back empty the row goes red with `named_count: 0` on both, which is the honest reading. The unscoped candidate list is compared whole, field for field, with ORDER normalised only — MusicBrainz returns tie-scored artists in an order two independent live queries do not agree on."
  },
  "GET /MusicGenres/{genreName}": {
   "category": "verified",
   "classification": "verified 2026-08-31 (batch F2, revised after review): 3/3 legs clean, 34 fields compared, measured twice. This row had NO classification before F2 and one probe leg.\n\n`MusicGenresController.GetMusicGenre` carries the SAME slug branch as the genres controller (`GetItemFromSlugName<MusicGenre>`, '-'->'&'/'/'/'?', three lookups, creating nothing) but NOT the same fallback: it keeps `if (item is null) return NotFound();` on BOTH trees (v10.11.8 :164-167, master :165-168, where the action is additionally `[Obsolete(\"Use GetGenre instead\")]`), where `GenresController` has `item ??= new Genre()`. That asymmetry is upstream's and it is the port. It is also how the wire caught a wrong port: the first cut copied the genres fallback here and answered 200 where a live 10.11.8 answers 404 (`/MusicGenres/R-B` J=404) — no unit test would have found it, because the two controllers' C# is identical until the last five lines.\n\nREAL WIRE DIVERGENCE FOUND AND FIXED IN THE REVIEW PASS (the reason this row is 34 fields and not 28). The two 404 legs used to be scored clean by a harness branch that treated 'both servers, same status, neither body JSON' as agreement — but `reads.py::token_get` discarded the body of EVERY non-200, so 'neither body is JSON' was a property of the probe, not of the responses. Measured directly with curl: J returned `{\"type\":\"https://tools.ietf.org/html/rfc9110#section-15.5.5\",\"title\":\"Not Found\",\"status\":404,\"traceId\":\"00-…\"}` and H returned `{\"error\":\"not found: music genre R-B\"}`. Two different documents, scored as one clean leg.\nFixed on BOTH sides. Harness: `token_probe` keeps the body at any status and the multi legs diff it like any other body, so an error body is now part of the comparison; a leg where neither server sent ANY body is recorded as `status-class` evidence (the ledger's weakest verdict) rather than as a body diff, and two different non-JSON bodies behind the same status are a mismatch. Server: `ApiError::Problem(ProblemStatus::NotFound)` renders ASP.NET's RFC 9110 ProblemDetails, and this handler uses it because its C# `NotFound()` takes no argument — ASP.NET then fills the body from `ApiBehaviorOptions.ClientErrorMapping`.\nIt is deliberately a PER-CALL-SITE variant and not a global change to how Ferrofin renders errors, because Jellyfin's error bodies are not uniform. Measured on the live 10.11.8 container in one session: `NotFound()` -> ProblemDetails; `GET /Users/{unknown}` (`NotFound(\"User not found\")`) -> the bare JSON string `\"User not found\"`; a model-binding failure -> a `ValidationProblemDetails` with an `errors` map; an unauthenticated request -> an EMPTY 401 with no content type; a wrong-verb request -> an empty 405. A blanket ProblemDetails would have traded one divergence for four. Those four call-site families are un-ported and NOT owned by this batch — they belong to the rows that probe them, and they are now visible to any multi row that probes one, because the harness no longer throws error bodies away.\n`traceId` is the one field on the VOLATILE list for this: it is ASP.NET's per-request W3C trace-context id, which two independent instances cannot agree on and one instance never repeats. Scoped to that exact key; `type`, `title`, `status` and `errors` are all still compared, which is what makes an error-body diff mean anything."
  },
  "GET /Persons/{name}": {
   "category": "verified",
   "classification": "verified 2026-08-31 (batch F2, revised after review): 2/2 legs clean, 56 fields compared, measured twice. REPLACES a pre-F2 'accepted-divergence: no lazy create on GET' record that was wrong on both counts — `LibraryManager.GetPerson` is a plain `GetItemList` lookup on both trees and creates nothing (unlike `GetGenre`/`GetStudio`, which are `CreateItemByName`), and Ferrofin already matched it. That upstream asymmetry is recorded HERE so it does not become invisible: `/Persons/{unknown}` must NOT materialize a Person row, while `/Genres/{unknown}` and `/Studios/{unknown}` must materialize theirs.\n\nThe row was red for two ported-wrong COLUMNS, both now fixed. (1) `SortName`: J=\"Alice Parity\" H=\"alice parity\". `BaseItem.CreateSortName` is gated on `EnableAlphaNumericSorting`, which `Person` overrides to false, so a Person's sort name is the name verbatim. The rule now has one home, `kinds::sort_name_for`, which `upsert_item`'s fallback, `backfill_missing_sort_names` and the people repository's inserts all route through. (2) `Path` and `PresentationUniqueKey` were never written: `save_people`'s Person insert now writes `Person.GetPath` (the first-letter subfolder form) and `Person-{Name}`, and creates the metadata directory after commit — `/config/metadata/People/{A,B,C}/{Name}` now exists on disk exactly as it does in the Jellyfin container.\n\nSTARTUP REPAIR, and the bug the review caught in it. Rows an older build wrote are repaired by `people_repository::repair_person_items` (it reported 3 on the first boot and 0 on the second). As first written that pass rewrote `SortName` to the verbatim name for EVERY Person row whose sort name differed, without reading `ForcedSortName` — so a person carrying a user sort-title override would have had it destroyed on every startup, and the pass would never converge on that row. It also falsified the pass's own 'no-op on a database adopted from Jellyfin' guarantee, since Jellyfin stores `ModifySortChunks(ForcedSortName).ToLowerInvariant()` there (BaseItem.cs:544 on master, :536 on v10.11.8). Fixed: the SELECT now reads `coalesce(\"ForcedSortName\",'')` and a row carrying one keeps its `SortName` untouched (the `Path`/`PresentationUniqueKey` halves still run — those are derived columns no user owns), which is the same guard migration `0012_hermit_episode_sort_names.sql` and `upsert_item` already applied. The lab could not have caught it: all three of its people have an empty ForcedSortName. Pinned by `repair_person_items_never_overwrites_a_forced_sort_name`, which asserts both the survival of the override and that the second pass reports 0."
  }
 }
}
