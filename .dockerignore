# Keep the docker build context small — suite/perf/Dockerfile.ferrofin uses the repo root.
target/
.git/
suite/
data/
.claude/
# The RustCodeGraph index (2.4GB) made "load build context" take longer than
# the compile; none of these feed the server build.
.rcg/
.port/
brain/
